# 语音模型选型调研（2026-09-14）

> **触发**：AuK 选型讨论中，用户要求补齐「B站 / 小红书等大厂 + 海外模型」的横向调研。
> **性质**：事实清单 + 决策建议。所有数字标注来源，凡未查到的一律写"未查到"，不估算。
> **上一版**：`.workbuddy/memory/2026-09-08.md`（流式 TTS 首次横向对比，结论是维持 faster-qwen3-tts）

## 修订记录

| 版本 | 触发 | 改了什么 |
|---|---|---|
| v1 | 首轮调研（团队两路并行 + 自查） | 建立候选池；RVC 换不掉的证据；许可雷区 |
| **v2** | **用户逐条核验 + 一手来源回核** | **作废 MOSS-TTS 候选**（三处数据错）；**核实 IndexTTS2 许可**（结论反转）；**修正 CosyVoice 3 首包**；**补齐 MeanVC2 官方一手数据** |
| **v3** | **P1 实验执行完毕（本机 CPU 实测）** | **新增 §2.2 MeanVC2 实测**：音色/音高迁移均弱于 RVC、16kHz 硬天花板、**修正官方 RTF 口径（0.633 仅对 120ms 档成立）**、多线程零收益；§6.4 未验证项关闭 3 条 |

> **v2 的关键教训（比任何模型数字都重要）**：v1 里 MOSS-TTS 的三个数字（"1.7B" / "~5GB" / "TTFT 180ms"）**没有一个是编造的，但没有一个属于这个模型**。
> 180ms 属 **MOSS-TTS-Realtime**（另一套架构），且测于 **L20** 数据中心卡；Local 档 v1.5 实为 **4B**（官方 News 2026-06-18：骨干从 Qwen3-1.7B 换成 Qwen3-4B）。
> **把一个模型家族里 A 档的指标安到 B 档头上，能让一个候选凭空变成"最贴"。** 这是本轮最贵的错误。

---

## 0. 结论速览

| 链路 | 现役 | 调研结论 | 行动 |
|---|---|---|---|
| **实时变声（VC）** | RVC RealtimeVST | **换不掉**。竞品差距是结构性的，不是调参能追的 | 维持 |
| **实时变声 · 唯一新选项** | — | **MeanVC2**（18M / 110ms / 零样本 / Apache-2.0）官方提供 **CPU-only 单 exe**，README 直接写了 VB-CABLE 路由 | **P1 实测** |
| **实时 TTS 合成** | faster-qwen3-tts | 原定"最贴候选"MOSS-TTS **已作废**（见上）；剩下的候选**规模都不小于现役**，无明确更优项 | 维持 + 有条件复测 |
| **离线音色克隆** | Qwen3-TTS | 有 3 个在特定能力上更强（时长控制/48kHz/Voice Design） | 按需接入 |
| **语音编辑/去口音/增强** | demucs + DeepFilterNet | AuK / FireRedTTS3 能做但**显存出局** | 不做 |
| **云 API**（火山/ElevenLabs/Cartesia） | — | 首包延迟数据都好看，但**违反"不上云"红线** | 排除 |

**一句话**：这一轮真正的收获不是"找到了替代品"，而是**两条否决证据 + 一个 18M 参数的低风险新选项**——MeanVC2 是唯一一个官方已把 VB-CABLE 路由写进文档的候选，恰好就是本项目要走的路。

---

## 1. 评估尺子（硬约束）

| 约束 | 值 | 含义 |
|---|---|---|
| 显存 | 8GB 物理，**实际可用 ≈6.5–7GB** | 扣掉 Windows 桌面合成占用；且现役 Qwen3-TTS 4.41GB + whisper + RVC 已在争用 |
| 延迟目标 | 端到端 <200ms | 竞品与行业基准口径（`docs/product/00_产品界定` §4） |
| 采样率协议 | **24000 Hz 帧协议** | cascade 客户端"零改"的前提（MEMORY §4） |
| 部署位置 | **本机 / 局域网，不上云** | 产品红线，直接排除所有云 API |
| 语言 | 中文必须强 | 主场景 |
| 许可 | 需可商用 | 项目已开源（`github.com/mouxu66/voice-morph`） |

> ⚠️ **报数字时必须同时报三件事：哪个 checkpoint、哪张卡、哪种场景（是否带参考音）。** 缺任何一项，数字就可以被安到别的档上——v2 修订的起因正是如此。

---

## 2. 实时变声（VC）：为什么 RVC 换不掉

这是本次调研**证据最硬**的一节。

| 方案 | 算法延迟 | 端到端 | 参数 | 零样本 | 来源 |
|---|---|---|---|---|---|
| **RVC v2（现役）** | **~15ms** | 40ms 块下 **55–75ms**（不含网络） | — | ❌ 需训练 | clonemyvoice.io 2026 |
| **Seed-VC（流式）** | **343ms** | + ~100ms 设备侧 ≈ 440ms | 25M | ✅ | 同上 / seed-vc 官方 README（~300ms 算法 + ~100ms 设备） |
| **MeanVC（前代）** | — | **211ms** | 14M | ✅ | MeanVC2 官方 README |
| **MeanVC2** | — | **110ms**（流水线首包，40ms chunk） | **18M** | ✅ | **官方 README（已核）** |
| X-VC | 未查到具体 ms | — | 未查到 | ✅ | arXiv:2604.12456，SJTU |
| w-okada/voice-changer | 未查到 | — | 多模型壳 | 视后端 | GitHub |

**关键结论**：Seed-VC 的 343ms 是**方法本身的物理下界**——扩散/迭代去噪无法截断到低延迟而不崩音质。官方 README 自己写的是"算法延迟 ~300ms"，与第三方口径一致。项目里 `seed_vc/` 只能当**离线**备选，这一点原先的判断是对的。

> 2026 年的延迟瓶颈已从声学模型转移到**声码器**：HiFi-GAN 类 5–15ms/chunk，BigVGAN 全质量要 3–10 倍。所有 sub-100ms 系统都刻意选 GAN 声码器——这是设计取舍，不是实现细节。

### 2.1 唯一值得试的新东西：MeanVC2（已核官方一手数据）

`github.com/ASLP-lab/MeanVC2` / HF `ASLP-lab/MeanVC2` / arXiv:2606.09050（西工大 ASLP@NPU + UNSW + WeNet）

**能力（官方 README 原文口径）**：

- **18M 参数**（前代 MeanVC 14M；对比 RVC 的量级、VoxCPM2 的 2B —— 差两个数量级）
- **端到端流水线首包延迟 110ms**（40ms chunk）；前代 MeanVC 是 **211ms** ——即官方口径的"将近减半"
- **零样本**：不需要为每个音色训练，直接喂参考音
- **单 CPU 核 RTF < 0.633**；FRC（未来感知分块）使峰值显存**降约 60%**
- 技术点：未来感知分块（FRC）+ 通用音色令牌编码器（UTTE，对参考音质量鲁棒）+ **Mean Flows 单步 1-NFE** ODE
- **Apache-2.0**（已核 LICENSE badge）；训练数据含 Emilia

**★ 架构（决定了它的上限，也决定了它为什么"能进本项目"）**：

识别-合成范式，五段式：**流式 ASR（Fast-U2++ / WeNet）提 BNF → ECAPA-TDNN + WavLM 出说话人嵌入 → UTTE 交叉注意力检索音色 → 4 层 DiT（CFM）出 mel → Vocos 声码器 → 16 kHz 输出**。

**★★ 官方已提供 Windows CPU-only 单 exe，且 README 直接写了 VB-CABLE 路由**：

| 可执行文件 | 延迟 | 说话人输入 | 体积 |
|---|---|---|---|
| `40ms_40ms.exe` | 40ms chunk + 40ms future = **80ms** | WAV 文件（WavLM + ECAPA-TDNN） | Full |
| `120ms_40ms.exe` | 120ms chunk + 40ms future = **160ms** | WAV 文件 | Full |
| `40ms_40ms_npy.exe` | 80ms | 预抽取 NPY | 小 ~1.2GB |
| `120ms_40ms_npy.exe` | 160ms | 预抽取 NPY | 小 ~1.2GB |

> 官方原话："**Audio Routing**: Input from microphone or **VB-CABLE Output** (capture PC playback); output to headphones/speakers or **VB-CABLE Input** (send to other apps, e.g., set as mic in Tencent Meeting)." 要求 Windows 10+ / 8GB RAM / ~4GB 磁盘。
> **这几乎就是把本项目的链路写进了它的文档**——麦克风或 CABLE Output 进，CABLE Input 出。且**纯 CPU，不动显卡**。

**为什么它值得单独拎出来**：RVC 换音色的代价是"每个新音色都要训练一次"（项目为此建了整套 `finetune` 流水线）。MeanVC2 若真能零样本 + 110ms，等于**直接砍掉训练环节**，同时把整张显卡让给 TTS。且它保留了 **speaker-specific fine-tuning**（用 safetensors 预训练权重做初始化）——万一零样本相似度不够，还能退回"微调"路线，等于**两头都留着**。

**必须在实测中确认的三条风险**（**v3：三条已全部本机实测，结论见 §2.2**）：

1. **16 kHz 输出**（Vocos）。RVC 现役是 40k/48k。16 kHz 的带宽上限只有 8 kHz，**听感上大概率更闷**——这是 18M + 单步 ODE 的必然代价，不是配置问题。→ **实测确认：8 kHz 以上结构性为零，非配置可解**。
2. **相似度上限未知**。110ms 是**流水线延迟**，不是音质指标。"11 亿小时级"训练量换不来 18M 的表达力上限。**必须人耳 A/B，且用袋鼠参考音**。→ **实测确认：音色迁移全面弱于现役 RVC**（LTAS 0.914–0.936 vs RVC 0.958）。
3. **范式不同**：内容/节奏来自 BNF（ASR 瓶颈特征），音色来自 UTTE 检索。**它不像 RVC 那样完整保留源 F0/韵律**——语气会被 ASR 那一段重新"解读"一遍。对"游戏内实时变声"是加分还是减分，只有实测知道。→ **实测确认：音高迁移只做到 RVC 的一半**（F0 变化 −6Hz vs −13Hz），源音残留更明显。

### 2.2 MeanVC2 本机 CPU 实测（v3 新增，2026-09-14）

**环境**：Windows 本机 CPU（非 GPU）、torch 2.5.1+cpu、12 逻辑核；源音 `tts_models/ref/meituan_rat_002.wav`（异说话人 3.08s）→ 目标音色袋鼠；实验目录 `D:\meanvc2_exp`（不入库）。RVC 基线走项目现役 `m2_server.rvc_convert` + `logs/kangaroo_v2`。

**A. 音色/音高迁移（与 RVC 同源同目标对比）**

| 音频 | SECS¹ | **LTAS 谱形相关** | F0 中位 | 采样率 |
|---|---|---|---|---|
| 目标参考音（袋鼠） | 1.000 | 1.000 | 126.3 | 22050 |
| 源音 未转换 | 0.964 | 0.866 | 125.0 | 22050 |
| **RVC kangaroo_v2** | 0.981 | **0.958** | 112.1 | 48000 |
| MeanVC2 120ms preset | 0.954 | 0.936 | 119.2 | 16000 |
| MeanVC2 40ms preset | 0.960 | 0.919 | 119.3 | 16000 |
| MeanVC2 120ms / steps=10 | 0.958 | 0.914 | 119.6 | 16000 |

¹ **SECS 在本项目不可用作唯一判据**：项目 x-vector 的余弦量程塌缩（未转换底噪即 0.964，全量程仅 ~0.04），单看它会把"未转换"判得比"转换后"更相似。**必须配 LTAS**。已记 `docs/犯错指南.md` §3.13。

**结论**：**音色迁移 MeanVC2 弱于 RVC**（LTAS 差 0.022–0.044）；F0 只迁移一半；**加步数（3→10）无收益反降**；无削顶/无直流偏移。

**B. 实时余量（修正官方口径）**

| preset | 块时长 | 单块计算 | 块内 RTF | VC RTF(整段) | 算法延迟² |
|---|---|---|---|---|---|
| 120ms（1 线程） | 120ms | 40ms | 0.333 | **0.348** | 200ms |
| 120ms（12 线程） | 120ms | 40ms | 0.336 | 0.351 | 200ms |
| 40ms（1 线程） | 40ms | 38ms | **0.949** | 0.932 | **118ms** |
| 40ms（12 线程） | 40ms | 39ms | 0.967 | 0.973 | 118ms |

² 算法延迟 = chunk + block 前瞻(40ms) + 单块计算；未计网络/声卡缓冲。官方 exe 名里的 80ms/160ms 即是前两项之和，与本机实测吻合。

**三条修正官方口径的结论**：

1. **官方"单 CPU 核 RTF < 0.633"只对 120ms preset 成立**（实测 0.348）；**40ms preset 实测 0.932**，几乎贴着实时的边——**1 线程下只剩 5% 余量，任何后台负载都会掉帧**。
2. **多线程没有任何收益**（12 线程与 1 线程几乎完全一致）。per-chunk 张量太小，线程调度开销吃掉了并行度。含义有两层：**买不到余量**；但也意味着它**只吃一个核就能跑住**，可以和 TTS/其他负载共存。
3. **ASR 段不是瓶颈**：warm RTF 仅 0.062（120ms）/ 0.117（40ms）。（首测因含 JIT 首次 warmup 误得 RTF≈1，剔除热机后为 1/16。）

**C. 定位判定**：**互补而非替代**，且实测数据支持这个判定。RVC 仍是固定音色的最优解（音色/音高迁移都更强、48kHz 全带）；**MeanVC2 的真正价值是"零训练任意音色快试"**——17 分钟从 clone 到出声，不用碰 `finetune` 流水线、不占显卡。若要用于实时链路，**务必选 120ms preset**（40ms 档单核余量不足）。

---

## 3. 实时 TTS：候选池与现役对照

### 3.1 dots.tts（小红书 dots 团队 + 上交 X-LANCE）

**这是本次调研最被低估的模型。** 2B 全连续自回归（无离散 token），AudioVAE 48kHz，Qwen2.5 骨干，Apache-2.0，七个 checkpoint。

**官方实测（单 H800，bf16，N=99）—— 注意这里有个大坑：**

| 版本 / 场景 | 首块延迟 p50 / p90 | RTF p50 | 峰值显存 |
|---|---|---|---|
| SOAR / **text_only** | **69 / 79 ms** | 0.18 | 7.85 GB |
| SOAR / **voice_cloning** | **225 / 404 ms** | 0.20 | **7.86 GB** |
| MF(4步) / **text_only** | **68 / 78 ms** | 0.13 | 5.73 GB |
| MF(4步) / **voice_cloning** | **204 / 381 ms** | 0.15 | **5.74 GB** |

> **🚨 最重要的单条发现**：论文/新闻里流传的「首包 85ms / 54ms」是 **text_only（无参考音）** 场景。**音色克隆场景要 204–225ms(p50)、381–404ms(p90)** —— 因为得先跑 speaker encoder 处理参考音。差 3 倍。
> 而我们项目的**每一个** TTS 调用都带参考音。看榜单时如果只看 85ms，会得出完全错误的结论。

- 显存随长度增长：<10s → 5.65GB；<20s → 6.53GB；<40s → **7.86GB**；<82s → 10.51GB
- 官方支持真流式：`DotsTtsRuntime.generate_stream()` 逐块 yield；双流 `DotsTtsRuntimeDoubleStreaming`（逐文本 token 喂）
- 权重体积（HF `dots-studio/dots.tts-mf`）：model 4.40GB + vocoder 0.72GB + speaker_encoder 0.03GB = **≈5.15GB**
- **第三方 GGUF 量化存在**（`cstr/dots-tts-soar-GGUF`）：Q4_K **2.32GB** / Q8_0 3.13GB / F16 4.64GB。**注意：官方只发 bf16，GGUF 是社区转的，且流式路径能否走通未验证。**
- 输出 **48kHz**，与项目 24000Hz 协议冲突 → 需加重采样或改协议

### 3.2 VoxCPM2（面壁智能 OpenBMB）

- 2B，tokenizer-free 扩散自回归，MiniCPM-4 骨干，AudioVAE V2，**48kHz 输出 / 16kHz 参考输入**
- **30 语言 + 9 种中文方言**（四川/粤语/吴语/东北/河南/陕西/山东/天津/闽南）
- **Voice Design**：纯文字描述造音色（"一个年轻女性，温柔，语速稍慢"），无需参考音
- 三级克隆：基础克隆 / 可控克隆（带风格指令）/ 终极克隆（参考音 + 转录文本，音频续写）
- 流式：`generate_streaming()`；RTF **0.30 @RTX 4090**（标准 PyTorch）/ 0.13（nano-vLLM）
- 显存 **~8GB**；bitsandbytes 量化可再降 40–50%
- 训练 236 万小时；Seed-TTS-eval：EN WER 1.84% / ZH CER 0.97%，SIM EN 75.3 / ZH 79.5
- Apache-2.0，GitHub **31.4k stars**，支持 LoRA 微调
- ⚠️ 官方 README 自承：**Voice Design 和可控克隆每次结果有差异**，建议生成 1–3 次挑最佳

### 3.3 对照：现役 faster-qwen3-tts

本机 RTX 5060 8GB 实测：显存 **4.41GB**、整句 RTF 0.61、TTFA **cs=8 → 0.48s**。

| | 规模 | 显存 | 首包（**音色克隆**场景） | 测试卡 | 采样率 |
|---|---|---|---|---|---|
| **faster-qwen3-tts（现役）** | 1.7B | 4.41GB（本机实测） | 0.48–0.67s（**本机实测**） | RTX 5060 | **24kHz** ✅ |
| dots.tts-MF | 2B | 5.74GB | 204ms（p50） | **H800** | 48kHz ⚠️ |
| VoxCPM2 | 2B | ~8GB | 未查到 ms | **RTX 4090** | 48kHz ⚠️ |
| CosyVoice 3 | **0.5B** | ~4–6GB（社区） | **160ms–700ms**（区间极大，见下） | 多来源混合 | 24kHz |
| ~~MOSS-TTS-Local-v1.5~~ | **4B** | 未查到 | **TTFP 2075ms**（并发1） | 单数据中心卡（未标型号） | 48kHz 立体声 ⚠️ |

**读法**：现役的 4.41GB / 0.48s 是**唯一在本机实测过的数字**。其余候选的公开数字都测在算力是本机 3–8 倍的卡上（RTX 5060 Laptop ≈ 4090 的 ~1/3、H800 的 ~1/8），折算后可能落回 0.5s 量级——**与现役持平，未必更好**。这一条只能本机实测，无法从公开数据推断。

**另一个被 v2 修正的点**：v1 曾把 MOSS-TTS 的"24kHz → cascade 零改"当作隐性优势。**实际它输出 48kHz 立体声**（vLLM-Omni PR 里 `VLLM_OMNI_BENCH_AUDIO_SAMPLE_RATE=48000`、`CHANNELS=2`）。现在表里**只有 CosyVoice 3 是 24kHz**，这一格的信息量比看上去大。

---

## 4. 离线音色克隆：谁在特定能力上更强

| 模型 | 发布方 | 参数量 | 显存 | 独有能力 | 许可 | 8GB 可行性 |
|---|---|---|---|---|---|---|
| **IndexTTS2** | **B站 Index 团队** | 1.5B | 4GB(FP16) 起，推荐 8–16GB | **精确时长控制**（自回归首例）+ **情感/音色解耦** | **bilibili Model Use License**（非 Apache，但**可用**；见 §6.2） | ✅ |
| **VoxCPM2** | 面壁智能 | 2B | ~8GB | Voice Design + 48kHz + 30 语言 9 方言 | Apache-2.0 | ⚠️ 踩线 |
| dots.tts-soar | 小红书 | 2B | 7.86GB | 相似度最高（SIM 79.2 均值） | Apache-2.0 | ❌ 踩线 |
| FireRedTTS3 | 小红书 FireRed | **5.18B** | 权重 **20.78GB**，需 12GB+ | 24 语言 + **21 种中文方言** | Apache-2.0（零样本克隆限学术） | ❌ 出局 |
| **AuK** | 腾讯混元 | 1.5B+3B编码器 | **24.78GB**（见 `MEMORY.md` §4） | 统一生成+编辑 | MIT | ❌ 出局 |
| Qwen3-TTS（现役） | 阿里 | 1.7B | 4.41GB（本机实测） | Voice Design | Apache-2.0 | ✅ |

**值得注意**：IndexTTS2 的**精确时长控制**是项目现在完全没有的能力，对 `audiobook.py`（有声书）场景价值明确——配音要卡画面时长。且 4GB 显存起步，是这一档里最轻的。**许可已核实为可用**（§6.2），这一条此前是"待核"，现已解除。

---

## 5. 六个反直觉的发现

1. **「宣称支持流式」≠「能用」**。Seed-VC 宣称实时（README 写得很清楚），实测算法延迟 343ms，是方法下界。判定标准必须是**有实测首包 ms 数**，而不是"支持流式"这四个字。

2. **榜单数字的测试卡决定一切**。dots.tts 测在 H800、VoxCPM2 测在 4090、AuK 测在 A800-80G、MOSS-TTS-Realtime 测在 **L20**。本机是 8GB 笔记本卡。**所有首包数字在下结论前必须折算。**

3. **★ 「同一场景内」还不够，要问到哪个 checkpoint**。v1 把 MOSS-TTS-Realtime 的 TTFB 180ms 安到了 MOSS-TTS-Local v1.5 头上——**同一篇 README、同一个家族，但是两个不同的架构和两个不同的大小（4B vs 未标）**。以后记 TTS 指标一律写成「模型@尺寸 / 场景 / 卡」三元组。

4. **小红书一家出了两个方向相反的模型**：
   - `dots.tts` —— 全连续自回归，**为交互/流式而生**，48kHz，Apache-2.0
   - `FireRedTTS3` —— LLM-DiT，统一"生成+编辑"，5.18B / 20.78GB，和 AuK 同类
   **同一个团队（FireRed vs dots，都属小红书）做出了路线相反的两个东西**，说明"统一编辑"和"低延迟流式"目前确实是两条互斥的路线——这正好解释了 AuK 为什么进不了实时链路。

5. **48kHz 正在成为新一波模型的默认输出**（dots.tts / VoxCPM2 / FireRedTTS3 / **MOSS-TTS-Local v1.5** 全是）。项目 cascade 帧协议锁死在 24000Hz，这意味着**任何一次 TTS 后端更换都要动协议或加重采样**——这是集成成本里最容易被低估的一项。同理，**MeanVC2 的 16kHz 输出**是反方向的协议不匹配（对虚拟声卡无碍，但有带宽上限）。

6. **许可不是二元的**。「Apache-2.0 ↔ 非商用」这个二分法会误判——IndexTTS2 是 **bilibili Model Use License**：**既不是 Apache，也不是 NC**，而是"免费授权 + 极高阈值（1 亿 MAU / ¥10 亿营收）才需另议"。判断许可要读**原文条款**，不能靠"是/否开源"或"能不能商用"两个标签。

---

## 6. 补充：其余国内大厂 + 海外开源

### 6.1 国内其余厂商

| 厂商 / 模型 | 参数量 | 显存 | 流式 | 首包 / RTF | 零样本克隆 | 中文 | 许可 | 地址 |
|---|---|---|---|---|---|---|---|---|
| **MOSS-TTS-Realtime** | 未查到 | 未查到（官方测于 **L20**） | **实测** | **TTFB 180ms（warm）/ RTF 0.51** | ✅ | 优 | Apache-2.0 | `github.com/OpenMOSS/MOSS-TTS` |
| **MOSS-TTS-Local-Transformer-v1.5** | **4B**（骨干 Qwen3-4B） | 未查到 | 实测（vLLM-Omni，2026-06 支持） | **voice_clone TTFP 2075ms**（并发 1，英文语料，output-len 512） | ✅ | 优 | Apache-2.0 | 同上 |
| MOSS-TTS-Nano | 0.1B | **4 CPU 核可实时流式** | 实测 | 未查到 ms | — | — | Apache-2.0 | 同上 |
| **CosyVoice 3** | 0.5B | ~4–6GB（社区） | **宣称** bi-streaming | **160ms–700ms（区间极大，见下）** | ✅ 3–10s | 优（9 语 + **18 方言**） | Apache-2.0 | `github.com/FunAudioLLM/CosyVoice` |
| **Step-Audio-TTS-3B** | 3B | 最低 8GB | 宣称 | 宣称 <300ms | ✅ 3–15s | 优（12 语 + 23 方言） | Apache-2.0 | 阶跃星辰，2026 |
| **Confucius4-TTS** | 1.3B | 未查到（权重包 54GB） | 未确认 | — | ✅ 3s，**免参考文本** | 优（14 语跨语种） | Apache-2.0 | `github.com/netease-youdao/Confucius4-TTS` |
| 字节 **MegaTTS3** | 0.45B | 未查到（约数 GB） | ❌ 扩散非流式 | — | ⚠️ **WavVAE 编码器未开源** | 优 | Apache-2.0 | `github.com/bytedance/MegaTTS3` |
| 字节 Seed-VC | 25M / 98M / 200M | 低 | 实测 | **~300ms 算法 + ~100ms 设备** | ✅ 1–30s | 良 | Apache-2.0 | `github.com/plachtaa/seed-vc` |
| GPT-SoVITS v4.1 | ≤1B | 8GB 可训 | ❌ 非实时 | RTF 0.014@4090 | ✅ 5s / 1min 微调 | 优（48kHz） | **MIT** | `github.com/RVC-Boss/GPT-SoVITS` |
| 智谱 GLM-4-Voice | 9B | ~18GB+ | 实测（流式 decoder） | — | ❌ 对话式 | 优 | Apache-2.0 | `github.com/THUDM/GLM-4-Voice` |
| 字节 Seed-TTS | — | — | — | — | — | — | **从未开源**（仅论文 + 评测集） | — |
| 豆包 / MiniMax Speech | — | — | 宣称 | MiniMax 宣称 TTFT 10ms | API | 优 | **未开源**（API） | — |
| 快手 / 百度 / 昆仑万维 / 商汤 | — | — | — | — | — | — | **未查到可确证的开源语音克隆权重** | — |

#### CosyVoice 3 首包：为什么写"160–700ms"而不是"~150ms"

v1 写的"宣称 ~150ms"过于干净。实际公开数据是**三个数量级不同的口径**：

| 口径 | 数字 | 场景 |
|---|---|---|
| 厂商渠道文章 | 首包从 **400ms → 160ms** | 双向流式合成架构（厂商自述） |
| vLLM-Omni 优化记录 | 流式 TTFP **~810ms → ~195ms** | **中文声音克隆，warm**（深度优化后） |
| 第三方基准（datascale-ai） | 均值 **~700ms**；短句 575ms / 中句 485ms | 通用部署 |

**读法**：195ms 是**经过 vLLM-Omni 深度优化**的数字，通用场景约 700ms。**CosyVoice 3 的延迟高度依赖部署优化程度**，不能引用单一数字。这也解释了一个反直觉现象：**同一个模型，换个推理框架，首包能差 4 倍**——所以"本机实测"这条纪律在 TTS 上尤其不能省。

#### MOSS-TTS 三档为什么被拆开

| 档 | 架构 | 关键数字 | 能否作为本项目候选 |
|---|---|---|---|
| **Realtime** | 独立架构，为低延迟设计 | TTFB **180ms**（warm）、RTF 0.51，**测于 L20** | 数字好看但**未标 LLM 规模/显存**，且 L20 非消费卡 |
| **Local-Transformer-v1.5** | `MossTTSLocal`，骨干 **Qwen3-4B** | **voice_clone TTFP 2075ms**（并发1，英文）、RTF 0.474 | ❌ **4B + 48kHz + 2 秒级首包**，三条都不达标 |
| **Nano** | 0.1B | 4 CPU 核实时流式 | ⚠️ 纯文本可用，克隆能力未查 |

> ⚠️ **额外发现（vLLM-Omni PR #4664 实测记录）**：Local v1.5 在**并发不同文本**时 6/6 次产出错误音频（请求间状态串扰，被并行调度时的 row 分配 bug），官方 reviewer 明确要求"修好之前不要依赖并发服务"。本项目是单人单路，暂不影响；但**说明这个模型当前工程成熟度偏低**。

### 6.2 ⚠️ 商用许可雷区

**这一节的工程价值高于所有性能数字**——项目已开源（`github.com/mouxu66/voice-morph`），用了 NC 权重等于埋雷。

| 模型 | 曾经的印象 | **实际许可** |
|---|---|---|
| **Spark-TTS**（讯飞 / HKUST） | 社区普遍按 Apache-2.0 引用 | **已改为 CC BY-NC-SA 4.0（非商用）** |
| **Fish Audio S2 / S2.1** | 常被当成开源 | **Fish Audio Research License**（商用需单独协议）；旧版 1.5 才是 Apache |
| **F5-TTS** | "代码 MIT，很宽松" | 代码 MIT / **权重 CC-BY-NC-4.0** |
| MaskGCT / Llasa / E2-TTS | — | **CC-BY-NC-4.0** |
| OpenAudio S1-mini | — | **CC-BY-NC-SA-4.0**（传染性） |
| XTTS-v2 | — | **CPML 非商用**（且已停止维护） |
| Higgs Audio v2 | 仓库显示 Apache-2.0 | 代码 Apache / **权重 "other"**；v3 明确非商用 |

**根因**：CC-BY-NC 的来源大多是 **Emilia 数据集**（10.17 万小时，CC-BY-NC-4.0），F5-TTS / MaskGCT 及一长串模型的权重都被它传染。**"代码 MIT" ≠ "权重能用"**——README 通常只引用代码许可。

#### ✅ 已核实：IndexTTS2 的许可（v1 标"待核"，v2 结论反转）

**读了仓库 LICENSE 原文**（`github.com/index-tts/index-tts`），确认不是什么 Apache-2.0，而是 **bilibili Model Use License Agreement**。但**它也不是非商用许可**——v1 的"⚠️ 存疑"和"需单独商用授权"两句话都不准确，实际条款是：

| 条款 | 内容 | 对本项目的影响 |
|---|---|---|
| §2.1 | 授予全球、非独占、不可转让、**免版税**的有限使用许可 | **可用** |
| §2.2 | **仅当**你或关联方「前一日历月月活 > **1 亿**」**或**「前一日历年营收 > **人民币 10 亿**」时，才必须另行申请授权 | **本项目远未触及阈值 → 无需另行授权** |
| §3.4(a) | 分发衍生作品时须对下游施加同等条款 | 若把 IndexTTS2 权重打进发行包，需附协议 |
| §3.4(b) | 每份副本须保留原始版权声明与本协议副本 | 需带 NOTICE |
| **§3.4(c)** | **不得用 IndexTTS2 或衍生作品去改进任何其他 AI 模型**，除非被改进的是 IndexTTS2 自身、其衍生作品、或**非商用** AI 模型 | ⚠️ **这条要留意**：不能用它蒸馏/训练自己的模型 |
| §4.1(a) | 分发衍生作品须注明"原作者不背书、不担保" | 需加免责声明 |
| §4.2 | 禁止用于医疗诊断/自动驾驶/军事/关键基础设施/大规模生物特征监控/自动化决策等高风险场景 | 本项目用途无关 |
| §6 | 适用**中国法律**，争议提交**上海仲裁委** | — |
| §9 | **中英文本冲突时以中文为准** | — |

> **结论：IndexTTS2 可以用于本项目**，但必须当作"**有附加义务的宽松许可**"对待，不是 Apache-2.0 的自由度。上线前要在 `README` / NOTICE 里带上版权声明与免责声明，且**不得用它做"改进其他模型"的用途**。

> **可商用且中文克隆强的安全区**（Apache-2.0 / MIT）：CosyVoice 3、MOSS-TTS 全系、Confucius4-TTS、Step-Audio-TTS-3B、GPT-SoVITS、MegaTTS3、Chatterbox、Kokoro、Orpheus、Zonos、dots.tts、VoxCPM2、**MeanVC2**、IndexTTS2（有条件）

### 6.3 海外开源（结论：对本项目基本无增量）

| 模型 | 参数量 | 显存 | 真流式 | 零样本克隆 | 中文 | 权重许可 | 备注 |
|---|---|---|---|---|---|---|---|
| **Chatterbox**（Resemble） | 0.5B | ~4–6GB | 分块流（宣） | ✅ ~5–10s | ✅ 23 语 | **MIT（代码+权重）** | 海外里许可最干净的可商用克隆模型 |
| **Orpheus**（Canopy） | 3B | ~8–12GB | 真流式（token-streaming） | ✅ | ⚠️ 英文为主 | Apache-2.0 | 英文情感表达强 |
| **Zonos v0.1**（Zyphra） | 1.6B | ~4–6GB | 宣 | ✅ 10–30s | ⚠️ | Apache-2.0 | Zonos2 是 8B MoE，需 16GB+ |
| **NeuTTS Air**（Neuphonic） | ~0.7B | GGUF Q4 **可跑 CPU** | ✅ | ✅ 3–15s | ⚠️ 有限 | Apache-2.0 | 唯一 CPU 可用的克隆模型 |
| **Kokoro-82M** | 82M | ~2–3GB | ✅ | ❌ **无克隆** | ✅ 8 语 | Apache-2.0 | 只能当兜底/预览音 |
| **VibeVoice**（微软） | 1.5B | ~6–10GB | ❌ | ✅ | ✅ EN+ZH | MIT | **模型卡明确写"Not for real-time applications"**；未支持重叠语音 |
| Sesame CSM-1B / Kyutai Moshi / Kyutai TTS | 1B / 7B / 1.8B | — | 全双工（Moshi） | ❌ 无克隆 | ⚠️ | Apache-2.0 / CC-BY-4.0 | 对话场景，不是换音色 |
| F5/E2-TTS、MaskGCT、Llasa、OpenAudio、XTTS-v2、Higgs v2 | — | — | 多数非流式 | ✅ | ✅ | **CC-BY-NC / CPML（非商用）** | 见 §6.2 |

**海外部分的总判断：对本项目没有增量。** 原因不是性能，而是三点错配——① 主流海外开源模型是**英文优先**，中文能力普遍不如国产；② 可商用的那几个要么**不支持克隆**（Kokoro / CSM / Moshi），要么**不支持实时**（VibeVoice 模型卡明写）；③ 支持克隆 + 流式的（F5-TTS / OpenAudio / XTTS）**权重全是 NC**，与项目已开源的定位冲突。

唯一值得记一笔的是 **Chatterbox**（MIT、0.5B、23 语、~4–6GB）——如果哪天需要多语言（非中文）克隆，它的许可最干净。

### 6.4 仍未验证

| 项 | 状态 |
|---|---|
| **各候选在 RTX 5060 8GB 的真实首包** | **全部未实测**——这是唯一能定论的证据 |
| ~~**MeanVC2 的真实相似度**~~ | ✅ **已实测（§2.2）**：音色迁移弱于 RVC（LTAS 0.914–0.936 vs 0.958）；F0 只迁移一半；16 kHz 8kHz 以上结构性为零 |
| ~~MeanVC2 的 ASR 段实际开销~~ | ✅ **已实测（§2.2）**：warm RTF 0.062（120ms）/ 0.117（40ms），**不是瓶颈** |
| ~~单 CPU 核 RTF < 0.633 是否属实~~ | ✅ **已实测**：120ms 档 0.348 ✅ 成立；**40ms 档 0.932 ❌ 不成立**（余量仅 5%） |
| MOSS-TTS-Nano 的克隆能力 | 未查（只确认 4 CPU 核可实时流式） |
| MOSS-TTS-Local v1.5 的中文 voice_clone 首包 | 官方基准是**英文**语料（`--locale en`），中文未测 |
| dots.tts GGUF 流式路径 | 未验证（官方只发 bf16，GGUF 为第三方转换） |
| Step-Audio-TTS-3B 的 <300ms | 厂商宣称，无第三方实测 |
| ~~IndexTTS2 真实许可~~ | ✅ **已核实**（§6.2），结论：可用，非 Apache，有附加义务 |
| ~~MeanVC2 原始出处~~ | ✅ **已核实官方 README + arXiv:2606.09050** |
| ~~MOSS-TTS v1.5 的 180ms / RTF 0.51~~ | ✅ **已澄清**：属 **Realtime 档**（L20 实测），非 Local 档 |

---

## 7. 建议（按性价比排序）

### P0 · 保持不动
- **实时变声继续用 RVC**。§2 的数据是结构性的，不是调参能追的。
- **TTS 继续用 faster-qwen3-tts**。在本机实测出更优项之前，没有理由换——现役是唯一有本机实测数字的（4.41GB / 0.48s），且是唯一 24kHz 的。

### P1 · MeanVC2 实验 —— ✅ **已执行完毕（2026-09-14），结果见 §2.2**

原计划要验证的三件事，**全部有答案**：

1. **相似度能否接近已训练的 RVC** → ❌ **不能**。LTAS 0.914–0.936 vs RVC 0.958。
2. **16 kHz 输出在游戏语音里能不能接受** → ⚠️ **结构性受限**（8kHz 以上为零），但游戏语音本就窄带，**主观听感待用户拍板**（音频已产出）。
3. **单 CPU 核 RTF<0.633 是否属实** → ⚠️ **分档**：120ms 档 0.348 属实；40ms 档 0.932 不属实。

**产出**：`D:\meanvc2_exp`（不入库，throwaway 实验沙箱）——含可复现 venv、`run_infer.py`（funasr 桩）、`ab_rvc_baseline.py`、`ab_metrics2.py`（SECS+LTAS+F0 三轴）、`rt_bench.py`（分块时延/RTF 基准）。

**仍未做**：与现役链路（`wechat_voice.py` 的离线变声路径）的实际接线与端到端试听——客观指标已明确不利于 MeanVC2，**在用户听完试听音频并认定"值"之前不必接线**。

**结论修正**：P1 从"投入小、上限可能很高"改为 **"已确认可用的零训练快试通道，但不是 RVC 的替代品"**。真正的价值在**任意音色免训练试听**（17 分钟从 clone 到出声、不占显卡、不动 `finetune` 流水线）。


### P2 · 有条件复测（不再有"最贴候选"）
- **MOSS-TTS 已作废**（v2）：其 Local 档是 **4B / 48kHz / TTFP 2075ms**，三条硬约束全不达标；180ms 属另一档且测于 L20。**除非官方给出 Local 档的中文克隆首包实测，否则不再作为候选。**
- 若要继续测 TTS，**首选 CosyVoice 3（0.5B）**——它是候选池里**唯一规模显著小于现役（1.7B）且输出 24kHz** 的模型，是"规模换延迟"这条路上唯一可能赢的赌注。但必须先固定推理框架，否则 160ms 与 700ms 的差距（§6.1）会让实测没有可比性。
- **先决条件**：任何 TTS 实测都必须带参考音测（参照 dots.tts 的教训），且必须记录本机首包与显存。

### P3 · dots.tts 本机实测（需先解决 48kHz）
- 先下 MF 权重（≈5.15GB），跑 `generate_stream` 的 **voice_cloning** 场景（不是 text_only！），量本机首包
- **前置条件**：48kHz → 24kHz 的重采样方案必须先定，否则改不了协议
- 若本机首包 ≥ 现役 0.48s，直接放弃，不纠缠

### P4 · IndexTTS2 作为离线能力补充（与主链路无关）
- 只为了 **精确时长控制**（有声书场景卡画面时长）
- 4GB 显存起步，可与主链路分时复用，不动实时链路
- ✅ **许可已核实为可用**（§6.2）：免版税授权，**仅"月活>1 亿 或 年营收>¥10 亿"才需另议**；但需带版权声明 + 免责声明，且**不得用于改进其他 AI 模型**（§3.4(c)）

### 不做
- ❌ FireRedTTS3 / AuK / Higgs v2 / Zonos2 / GLM-4-Voice —— 显存出局
- ❌ **MOSS-TTS-Local-Transformer-v1.5** —— 4B + 48kHz + 2 秒级首包（v2 新增）
- ❌ 所有云 API —— 违反"不上云"红线
- ❌ Seed-VC 做实时 —— 343ms，结构性不达标
- ❌ 任何 **CC-BY-NC / CPML** 权重（F5-TTS / MaskGCT / Llasa / OpenAudio / XTTS / Spark-TTS / Fish S2）—— 项目已开源，不能埋许可雷

---

## 附：来源

| 事实 | 来源 |
|---|---|
| dots.tts 全部实测数字、checkpoint 清单、Apache-2.0 | `github.com/rednote-hilab/dots.tts` README；arXiv:2606.07080 |
| dots.tts 权重体积 | HuggingFace API：`dots-studio/dots.tts-mf`、`cstr/dots-tts-soar-GGUF` |
| IndexTTS2 参数量/显存/时长控制 | `github.com/index-tts/index-tts`；arXiv:2506.21619；官方 config.yaml |
| **IndexTTS2 许可条款（逐条）** | **`github.com/index-tts/index-tts` 仓库 LICENSE 原文 —— bilibili Model Use License Agreement（§2.2 阈值 / §3.4 义务 / §4.2 禁区 / §6 法律适用）** |
| FireRedTTS3 5.18B / 20.78GB / 21 方言 | ModelScope `FireRedTeam/FireRedTTS3`；arXiv:2608.17492 |
| VoxCPM2 全参数 | `github.com/OpenBMB/VoxCPM` README |
| AuK 显存/许可/prompt adherence | `github.com/Tencent-Hunyuan/AuK`；`github.com/Saganaki22/ComfyUI-AuK`；HF `drbaph/AuK-comfyui` |
| Seed-VC 343ms / RVC 15ms / 延迟预算分解 | clonemyvoice.io「Live Voice Cloning Latency: Seed-VC vs RVC v2 in 2026」；seed-vc 官方 README |
| **MeanVC2 110ms / 18M / 211ms / CPU RTF / FRC 60% / exe 清单 / VB-CABLE 路由 / Apache-2.0** | **`github.com/ASLP-lab/MeanVC2` 官方 README（一手）；arXiv:2606.09050** |
| X-VC | arXiv:2604.12456；`github.com/Jerrister/X-VC` |
| 火山引擎/豆包 Seed-TTS 2.0 数据 | 火山引擎开发者社区 2026 年选型文（**厂商渠道，数字需打折看**） |
| **MOSS-TTS 家族许可 / 采样率 / exe 与 8GB 配置 / Realtime TTFB 180ms@L20** | **`github.com/OpenMOSS/MOSS-TTS` 官方 README（含 News 2026-06-18 骨干升级说明）** |
| **MOSS-TTS-Local-v1.5 的 voice_clone 基准（TTFP 2075/3709/8797ms、RTF 0.474/0.718/1.758、output-len 512、locale=en、48kHz 立体声、并发串扰 bug）** | **vLLM-Omni PR #4664 原文（`github.com/vllm-project/vllm-omni/pull/4664`）** |
| **CosyVoice 3 首包多口径（160ms / 195ms warm / ~700ms）** | 厂商渠道文章 + vLLM-Omni 优化记录 + datascale-ai 第三方基准（**用户核验提供**） |
| 国内其余厂商（CosyVoice3 / Step-Audio / Confucius4 / MegaTTS3 等） | 各官方 GitHub 与 HuggingFace 模型卡 |
| **许可雷区**（Spark-TTS / Fish S2 / F5-TTS / MaskGCT / Llasa / OpenAudio / XTTS / Higgs） | builderai.tools「Open-Weight TTS in 2026」；bittide.aicompass.dev TTS 清单；各 HF 模型卡 license 字段 |
| 海外开源（Chatterbox / Orpheus / Zonos / NeuTTS / Kokoro / VibeVoice 等） | 各官方仓库与模型卡；VibeVoice「Not for real-time」出自其模型卡限制章节 |
