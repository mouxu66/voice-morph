# 开源前代码审查 · 待办清单

> 日期：2026-09-12 | 范围：`m2_server`(177 py) / `web/src`+`web/electron`(79 ts+tsx+cjs) / `tools` / `scripts` / `docs` / `README`
> 方法：只读审计（未改任何代码）。执行了 `ruff --select F,E9`（全绿）、`git ls-files` 全量遍历、凭据/path/TODO/console 正则扫描。
> 基线：仓库 448 文件入库、pack 14.62 MiB、无 >1MB 大文件、`ruff F,E9` 0 告警。

---

## 结论速览

| 级别 | 数量 | 性质 |
|---|---|---|
| 🔴 P0 必须处理 | **3** | 会直接造成「开源即事故」 |
| 🟠 P1 强烈建议 | **5** | 影响可信度 / 新用户开箱体验 |
| 🟡 P2 可选优化 | **6** | 洁癖级，可转 issue |
| ✅ 已验证良好 | **9** | 不用动，列出来省得重复扫 |

**总体判断：这份代码库的开源就绪度远高于一般个人项目。** 五类里第 1 类（调试残留）和第 2 类（异常处理）基本及格，真正的风险全集中在 **第 3 类「机器专属痕迹」** 和 **第 4 类「文档与实现脱节」**——不是代码脏，而是「你自己的机器指纹留在里面了」。

> **执行状态（2026-09-14）**：P0 / P1 / P2 三批**全部完成**（见 §六），
> 仓库已公开（<https://github.com/mouxu66/voice-morph>），CI 已绿。
> 第 14 项转出的 3 个 issue 里 **#3 也已闭环**（§3.6，含"不重写历史"的结论），
> 只剩 #1（移动端 mDNS 自动发现）/ #2（共享 API 客户端去重）——
> 它们是"改进项"，不是"开源前阻塞项"。下面各节的结论保留当时原貌，未回改。

---

## 一、调试代码 / console.log / TODO

### ✅ 结论：这一类基本干净，只有 4 处需要判断

| # | 位置 | 内容 | 建议 |
|---|---|---|---|
| 1.1 | 全仓 `*.py/ts/tsx/cjs` | **零真 TODO/FIXME**。命中的 3 条全是误报：`auto_pipeline.py:31` 是 `xxx.mp4` 占位示例、`gen_vc.py:1` 是英文单词 "hack" 出现在注释里、`gen-selfsigned-cert.ps1:21` 是 `-Password xxx` 用法示例 | 无需处理 |
| 1.2 | `web/electron/backend.cjs` 等 6 文件 | 13 处 `console.log`，**全部是主进程日志**（`[backend] 开发模式后端根…`、端口占用提示） | **保留**，主进程无 UI，属于必要日志。可选：抽个 `debug()` 开关 |
| 1.3 | `m2_server/` 61 处 `print(` | 大量 `[cascade]`、`[capture]` 日志。这些是**子进程/长任务**输出，被 `backend.cjs:346` 捕获转发 | **保留**，但见 4.4 一致性建议 |
| 1.4 | `web/electron/make-update-manifest.cjs:123-126` | CLI 脚本的 `console.log` | **保留**，命令行工具输出正确 |

> **判定**：这一项不需要清理动作。真正的「调试残留」是下面两个文件：`smoke_out.log` / `smoke_err.log` / `smoke_style_*.log` 已在 `.gitignore`（`*.log`），**未入库**，但堆在仓库根目录。

### 1.5 🟡 遗留 asar 脚本（AGENTS.md 明令禁止再跑）

| 文件 | 状态 |
|---|---|
| `extract_asar.cjs` / `probe_asar.cjs` / `repack_asar.cjs`（仓库根） | 已入库，**内部硬编码 `D:/变声/...` 绝对路径**，且 AGENTS.md 第 3 条明写「`.asar_tmp/` + repack_asar.cjs 是过时流程，**禁止再跑**（会拿 46KB 旧单体主进程覆盖现役装配层）」 |
| `tools/verify_asar_repack.cjs` | 同类，也硬编码 `D:/变声/voice-morph-desktop/resources/app.asar` |

**建议**：删除这 4 个文件（或在文件头加 `// DEPRECATED — 勿用，见 AGENTS.md` 并移入 `tools/_deprecated/`）。开源后别人看到「复现步骤里有 repack_asar.cjs」会照跑，直接毁掉他的安装包。

---

## 二、错误处理是否合理（不能吞异常）

### ✅ 结论：**整体优秀**，静默异常绝大多数带注释说明为什么可以吞

`ruff --select F,E9` 全绿；无裸 `except:`（0 处）；无 `except Exception: pass` 压缩写法（唯一 1 处命中在 `web/node_modules`，非项目代码）。

抽查到的吞异常全部**有明确契约**，例如：

- `clip_qc.py:170` `except Exception:  # noqa: BLE001` + `return None`
- `finetune.py:464` `except Exception:  # noqa: BLE001 —— 体检失败绝不阻断训练`
- `clips_api.py:22` `except Exception:  # 质检结果读不到不影响列表本身（无质检时前端退化为按响度筛选）`
- `cascade_stream.py:71` `_live_input_keyword()` 读设置失败 → 回退旧行为「绝不阻塞采集」

API 层有 103 处 `HTTPException`，异常到 HTTP 语义的转换做得规范（`clips_api.py:93-99` 甚至给了可读原因 + 下一步建议）。

### 2.1 🟠 前端 5 处 `catch(() => {})` 完全静默，其中 1 处会造成「状态不确定」

| 位置 | 问题 | 严重度 |
|---|---|---|
| `web/src/pages/Tts/RvcDatasetCard.tsx:13` | `getRvcModel().catch(() => {})` → 请求失败时 `m` 保持 `null`，`ready = m?.trained ?? false` = **false**，UI 显示「未就绪」。**后端挂了和真没训练，用户看到的文案一模一样** | 🟠 会误导 |
| `VoiceMarket/VoiceMarketPage.tsx:488` | 回滚失败静默 → 用户以为成功 | 🟡 |
| `VoiceMarket/VoiceMarketPage.tsx:502` | 卸载失败静默 | 🟡 |
| `components/voice-studio/WorksLibrary.tsx:88` | `historyTags()` 失败静默，标签不显示 | 🟡 |
| `api/client.ts` 7 处 `catch { /* ignore */ }` | **合理**：都是「解析错误响应体拿 detail，失败就用 statusText」 | ✅ 保留 |

**建议**：至少给 `RvcDatasetCard` 加 `setErr()` 分支，把「检测失败」和「未就绪」分开——这是新用户第一次打开「实时变声」页就会看到的卡片。

### 2.2 🟡 `capture_api.py:32` / `mine_api.py:35` 后台线程只 `print`，无 `logging`

后台流程失败只打一行 stdout，日志轮转/级别控制都没有。低频，可接受。

---

## 三、硬编码路径 / 硬编码密码

### 3.1 🔴 代码签名证书私钥密码，**入库 3 处**（本机另有 1 处未跟踪）

> ⚠️ 本条目原本逐字引用了那个密码——**引用本身也是泄漏**，故 2026-09-13 把字面量
> 全部打码。原始值请到证书管理处取。

| 位置 | 内容 |
|---|---|
| `scripts/vm-test.config.ps1:24` | `$CertPassword = "<已打码>"` ← 仅存于本机，**该文件已在 .gitignore**（未跟踪） |
| `scripts/release.ps1:49` | `Warn "  设法：` `$env:CSC_KEY_PASSWORD = '<已打码>'` `（或从证书管理处取）"` ← **明文，已提交**（且会打印进发布日志） |
| `docs/release-sop.md:37` | `$env:CSC_KEY_PASSWORD = '<已打码>'` |
| `docs/release-sop.md:134` | 同上，第二次出现 |

> ✅ **2026-09-13 已处理**：3 处入库字面量全部清除（`release.ps1` 改为只给用法 +
> 指向 `scripts\gen-selfsigned-cert.ps1`，`release-sop.md` 改占位符），证书本体沿用；
> 并新增 `tools/check_secrets.py` 按**内容**拦截（原先 `.githooks/pre-commit` 只按
> 文件名拦密钥，所以它一路进了仓库）。
> ✅ **2026-09-13 13:18 历史也已清洗**：`git filter-repo` 重写 master
> （`9504e21` → `ce08f41`，首个被改提交 `c08d1e7` → `1b620047` = 那个引入发版 SOP
> 的提交），重写记录见 `.git/filter-repo/`。
> **已核实旧值确实不可恢复**：旧 HEAD 对象 `9504e21` 已查不到（`git cat-file` 报
> could not get object info）、`git fsck --unreachable` 为 0、对**全对象库**
> （`git cat-file --batch-all-objects`，含不可达对象）扫 `CSC_KEY_PASSWORD` /
> `CertPassword` 只剩占位符与测试夹具（`certpass123` / `s3cret-pass`）。
> 因此**本条已闭环，不再是转公开的前置阻塞**。
> 另：证书本体 `certs/*.pfx` 从未入库，故无需换证书；是否顺手换掉密码属于卫生问题，
> 由你自己定。

**缓解事实（不改变结论）**：
- `certs/black-seraph.pfx` **未入库**（`.gitignore:88` 已含 `certs/` + `*.pfx` + `*.p12`）
- 全量 git 历史（`git log --all --diff-filter=A`）**从未出现过任何 `.pfx`/`.p12`**

**所以严重度**：不是「私钥泄漏」，而是「**密码泄漏给全互联网**」。任何拿到你安装包的人，只要猜到你用自签证书，就能尝试用这个密码……

**处置建议（三选一，需你确认）**：
1. **最稳**：这个自签证书本来就是测试用、没进过任何公开信任链 → **直接废弃该证书 + 生成新证书 + 新密码**，然后从 4 处删除明文。
2. 若证书要留用：把密码改成从环境变量读（`release.ps1` 已经是这个模式，只需删掉 fallback 字面量），并从文档中抹掉。
3. 最低限度：改掉这 4 处明文，但**历史提交里仍有**，需要 `git filter-repo` 清历史。
   > 事后结论（2026-09-13）：最终选了这条 —— 因为 `certs/*.pfx` 从未入库，
   > 「换证书」没有意义（要防的是密码而不是密钥），而留着明文密码进公开历史
   > 是永久性的。重写成本实测很低（206 提交、本地库、无远端 → 无需通知任何协作者）。

> ⚠️ 顺带：`scripts/vm-test.config.ps1` 本身**已在 `.gitignore`**，但文件存在本地且内容里有 `$HostIP = "<作者本机 VMnet8 网关>"`。它没入库所以没问题——**但请确认它从未被 `git add -f` 过**（我查了历史，无记录 ✅）。

### 3.2 🟠 硬编码 `D:\变声` / `D:/RVC`——分级看

**A. 有 env 覆盖、属正常默认值（✅ 保留）**
- `m2_server/config.py:43` `RVC_ROOT = _path("VM_RVC_ROOT", Path("D:/RVC"))` — 有覆盖，且 `.env.example` 已写
- `m2_server/offline_vc_infer.py:35` `os.environ.get("VM_RVC_ROOT", r"D:\RVC")` — 有覆盖
- `tools/tts_and_send.py:40` / `tools/doctor.py:175` — 有覆盖

**B. 无覆盖、真硬编码（🟠 需处理）**

| 位置 | 内容 | 影响 |
|---|---|---|
| `web/electron/backend.cjs:38` | `"D:\\变声"` 在第 2 候选路径里 | 开源用户不会受影响（第 1 候选 `resources/backend` 命中），但代码里裸露你的机器路径 |
| `web/electron/backend.cjs:154` | `"D:\\变声\\.venv\\Scripts\\python.exe"` | 同上，作为候选可以保留但应注释「本机开发者专用」 |
| `web/electron/backend.cjs:238` | `const LEGACY_ROOT = "D:\\变声"` | 同上 |
| `tools/cascade_acceptance.py:30` | `RVC_PY = Path("D:/RVC/.venv/Scripts/python.exe")` | **无 env 覆盖** |
| `tools/verify_asar_repack.cjs:3` | `D:/变声/voice-morph-desktop/resources/app.asar` | 见 1.5，建议直接删文件 |
| `extract_asar.cjs` / `probe_asar.cjs` / `repack_asar.cjs` | 三条 `D:/变声/...` | 见 1.5 |
| `m2_server/pet_skin_build.py:3`、`qwen3_tts_service.py:26` | docstring 里的路径 | 仅注释，🟡 |

**C. 注释/docstring 里的路径（🟡 纯文案）**
`cascade_stream.py:3`、`ab_chain.py:105`、`offline_vc.py:8`、`rvc_convert.py:7`、`wechat_voice.py:26` 等 ~10 处写着「运行环境：`D:\RVC\.venv\...`」。作为「本机约定说明」有价值，但应补一句「开源用户请设 `VM_RVC_ROOT`」。

### 3.3 🟠 硬编码默认音色 `meituan_rat`（私有素材指纹）

| 位置 | 内容 |
|---|---|
| `m2_server/config.py:47` | `RVC_DEFAULT_EXP = _str("VM_RVC_EXP", "meituan_rat")` |
| `m2_server/cascade.py:47` | `DEFAULT_REF = ROOT/"tts_models"/"ref"/"meituan_rat_002.wav"` |
| `m2_server/cascade_stream.py:847` | `default=os.path.join(_root,"tts_models","ref","meituan_rat_002.wav")` |
| `m2_server/prosody_relay.py:24` | `DEFAULT_REF = Path(cfg.ROOT)/"tts_models"/"ref"/"meituan_rat_002.wav"` |
| `web/src/pages/Tts/RvcDatasetCard.tsx:47` | **UI 文案硬编码 `模型权重 meituan_rat.pth`** ← 后端返回的 `exp` 字段可用却没用 |

**好消息**：`tts_models/`、`media/`、`outputs/` **全部未入库**（`git ls-files` 为空），所以参考音文件本身没泄漏。
**问题**：开源用户 clone 后，`config.py` 那条注释写着「meituan_rat 是 2026-08-29 固定源 A/B（**源=用户本人录音**，pitch=0）中选定的最佳音色」——这是你的私人音色记录，且默认值指向一个陌生用户根本没有的文件。

**建议**：默认值改为 `""` 或 `default`，让 /rvc 相关功能在无音色时给出「请先创建音色」的引导；前端 `RvcDatasetCard` 改用 `m.exp` 渲染。

### 3.4 🟡 文档中的内网 IP 与用户名

- `docs/release-sop.md` 6 处 `http://192.168.1.100:9000`（原为作者本机 VMnet8 网关，**2026-09-13 已换成通用示例 IP**）
- `scripts/check-update-source.ps1:4`、`scripts/release.ps1:5` 注释示例
- `docs/AUDIO_TROUBLESHOOTING.md:80` `C:\Users\<用户名>\AppData\Local\Temp\`

虽然 `192.168.x.x` 不可路由、无实际风险，但属于机器指纹。建议替换为 `192.168.1.100` / `<你的用户名>`。

### 3.5 ✅ 凭据泄漏排查：**干净**

| 检查项 | 结果 |
|---|---|
| `.env` / `credentials.json` / `id_rsa` / `*.pem` / `*.key` 入库 | ❌ 无 |
| `gho_` / `ghp_` / `sk-` / `AKIA` 令牌字面量 | ❌ 无 |
| `BEGIN PRIVATE KEY` 块 | ❌ 无 |
| `certs/*.pfx` 入库 | ❌ 无（`.gitignore` 已挡；**git 全历史也无**） |
| 前端/后端 API Key 硬编码 | ❌ 无 |
| `.env.example` 是否只含模板 | ✅ 只含空值模板 |
| `pre-commit` 钩子是否拦密钥 | ✅ `core.hooksPath=.githooks` 已启用，钩子第一条就是密钥红线 |

### 3.6 ✅ 第三方素材名残留（issue #3）：**改工作区已完成**，是否重写历史待定

`git grep` 复核（2026-09-14）：入库文件里仍有 **9 个**带第三方素材名 ——
`experiments/m1_workshop/analyze_videos.py`、`experiments/m1_workshop/gen_vc.py`、
`experiments/tools/{asr_ab,measure_f0_ab,measure_speed_ab}.py`、`tts_trial/move_material.py`、
`tts_trial/sim_score.py`，加上两个**原始数据**文件
`m1_workshop/_clip_picks.txt` / `m1_workshop/_f0.json`。
（issue 里列的另外 4 个 —— `m2_server/enhance_calib.py`、`tts_trial/{ft_infer,ft_prepare,sft_8gb}.py`
—— 已经在 `f79e767` 及后续提交里清掉了。）素材**本体**全都没入库（`media/` 已挡），
泄漏的只是名字。

处置（对应 issue #3 的两点决策）：

| 类别 | 做法 | 理由 |
|---|---|---|
| 7 个**代码**文件 | 片名换成中性编号，沿用 `f79e767` 已立的约定（`第三方素材 01` / `第三方素材 02` …） | 代码里的片名只是个字面量，自己跑时改回去即可，代价极低 |
| 2 个**原始数据**文件 | `git rm --cached` + `.gitignore`（`m1_workshop/_*.{txt,json}`），**文件仍留在本机** | 它们是那次实验的原始输出、**没有任何脚本引用**，对克隆者毫无意义；改名会破坏可复现性，所以整类移出仓库，而不是改名 |
| 音色标识符（`kangaroo` / `feidudu`） | **不动** | 它们是项目自己的音色名（微调产物与 `--speaker_name` 都依赖），不是"素材文件名"；动它们要连带改微调链路 |

**机器守护**：`tools/audit_git_history.py` 新增第 3 节「第三方素材名命中」，规则从
**不入库**的 `.audit-materials.txt` 读（把片名写进仓库，等于泄漏源换了个位置、还更隐蔽）。
判定与降级语义的修复复盘见 `docs/犯错指南.md` §3.11。

**历史重写：决定不重写（2026-09-14）**。理由：

1. 历史里剩的只有第三方片名 + 一个用户名（`C:\Users\<用户名>` / `/Users/<用户名>/`），
   **没有任何凭据**（凭据 0 处）；
2. 用户名已因 GitHub 账号 `mouxu66` 公开而失去隐蔽价值；
3. 重写会把**所有已公开的提交哈希**改掉 —— 本文档与 `docs/犯错指南.md` 里引用的
   `f79e767` / `dc20ad3` / `a1d0f40` / `5dd1fdf` 等会**全部失效**，
   成本大于收益。

因此日常检查改用 `python tools/audit_git_history.py --allow-history-only`：它只放行
"仅历史 **且** 非凭据"这一类，**凭据类（任何位置）与「HEAD 仍含」（任何类别）照样失败**。

**当前基线（2026-09-14 提交后实测）**：`HEAD 仍含 0 处` / `仅历史 110 处`
（素材名 100 + 隐私 10；凭据 0）。`--allow-history-only` 退出码 **0**。
完整的重写方案（触发条件 / 备份与回滚 / 影响面量化 / 本机 refs 坑）见
`docs/开源-历史重写方案_2026-09-14.md`。

> **这道门真的拦住了新泄漏（同日稍后）**：`71138fd` 记 torch.jit.load 中文路径坑时，
> 把验证用的**真实**家目录路径写进了 §3.12 正文 —— 审计工具当场报
> `HEAD 仍含 5 处 → 要动的文件：docs/犯错指南.md`（另外 4 条 `[HEAD 另处仍含 → docs/犯错指南.md]`
> 也一并指了过来，它们命中的都是同一个片段）。改成 `C:\Users\<你的用户名>\` 后归零
> （提交 `fcd8d26`）。
> 教训：**文档里的"举例路径"最容易带出真用户名** —— 写的时候就该用占位符，
> 而不是等审计来抓。这也是它第一次拦下**新增**泄漏，而不是只翻历史旧账。

> 为什么"仅历史"的数字从 60 涨到 109：`audit_git_history.py` 的来源标记规则
> 2026-09-14 收窄到**文件名语境**（原来的"出现平台名即命中"会误报 IndexTTS2 的**许可名**，
> 逼着人把许可名从合规文档里删掉）。片段变长、判定变准，命中条数因此变化 —— 这是**判据改进**
> 带来的数字变化，不是新泄漏。修法与反例见 `docs/犯错指南.md` §3.11。

---

## 四、README / 注释与实际是否一致

### 4.1 🟠 `mobile/api.ts` 不存在，实际是 `mobile/src/api.ts`（**3 处引用错误**）

| README 行 | 原文 | 实际 |
|---|---|---|
| 148 | `` `mobile/api.ts` 调 PC 后端实现微信语音遥控 `` | 应为 `mobile/src/api.ts` |
| 320 | `` 改 `mobile/api.ts` 的 PC 局域网 IP `` | 应为 `mobile/src/store.ts`（host 存 store，默认值 `http://192.168.1.10:8000`） |
| 253 | `` 改 mobile/api.ts 里的 PC 后端地址 `` | 同上 |

> 移动端结构：`mobile/src/api.ts`（API 层）+ `mobile/src/store.ts`（**host 默认值在这里，line 21**）+ `mobile/app/(tabs)/`（4 个页面）。README 全篇把这两个文件混成一个不存在的路径。

### 4.2 🟠 router 数量不一致

- README:134 写「**9 个**独立 router」
- 实际：`m2_server/*.py` 里 `APIRouter(` 出现 **24 次**，`server.py` 有 **24 处 `include_router`**

README 同一段后半句列的（`system/voices/raw_media/pipeline/clips/tts/mine/capture/ab/audio/media/rvc_dataset` = 12 个）才是准确的。前面那个「9 个」是旧数字。**建议统一为 21 个能力 router**（12 域 + cascade/rvc_live/offline_vc/seed_vc/audiobook/effects/wechat_voice/finetune/history）。

### 4.3 🟠 README「技术栈」表与 `audio_config.ps1` 说明不符

- README:64 写「`audio_config.ps1` 调 Core Audio IPolicyConfig 切默认播放/录音设备」
- 实际 `m2_server/audio_config.ps1` 是 **PowerShell 内嵌 C#（`Add-Type`）** 实现 COM 接口，**不是** `comtypes`。项目记忆里记的是「改用 comtypes 直连」——**这条记忆与仓库现状不符**，README 反而是对的。

### 4.4 🟡 后端日志方式不统一（注释/文档未声明）

66 个后端 py 文件里只有 18 个用了 `logging`，其余用 `print()`；**全仓无 `logging.basicConfig`**。作为子进程被 Electron 捕获转发时这没问题，但开源用户直接 `python m2_server/server.py` 时没有日志级别控制。**建议 README 补一句「日志即 stdout，不做级别管理」**，而不是改代码。

### 4.5 🟡 `README-vm-test.md` 与 `docs/` 里的临时文档

- `README-vm-test.md`（172 行）是**你自己两台机器之间的 VM 测试手册**，含 `vmrun`、你的 HostIP、`D:\变声` 快照要求。开源后它出现在仓库根目录的 README 隔壁，会造成困惑。
- `docs/` 下 4 篇日期型临时文档：`代码审查-2026-09-05.md`、`代码审查-2026-09-11.md`、`功能完善建议-2026-09-05.md`、`清理清单-2026-09-07.md`、`性能排查报告.md`。
- `README.md:342` 自己写着「架构演进建议（**待办**，非当前阻塞）」——里面 2 条已划掉标 ✅ 完成，剩下 3 条（mDNS 自动发现、共享 API client、保持 Electron）应转 issue。

**建议**：`README-vm-test.md` → 移到 `docs/internal/`；日期型文档 → 保留（它们是诚实的过程记录，对开源项目是加分项），但在 `docs/README.md` 加索引说明哪些是历史快照。

### 4.6 ✅ 抽查一致的项

- README 引用的 `tools/setup_env.ps1`、`tools/doctor.py`、`tools/cleanup.ps1`、`web/electron/preload.cjs`、`m2_server/runtime.py` 全部存在 ✅
- README 自称「不是旧 README 写的 `release/`」而实际用 `release2/` — 自洽 ✅
- `web/electron/pet.html` 与 `alt-hint.html` 实际在 `web/electron/pet/` 子目录（README 未给完整路径，不算错）
- README 已声明 `web/src` 零 `console.log` — 实测**确实零**（只有主进程有）✅

---

## 五、其他发现（不在你列的五类里，但开源前值得看）

| # | 项 | 说明 |
|---|---|---|
| 5.1 | `.github/workflows/ci.yml` **已存在且设计良好** | Windows runner 跑 ruff+pytest、Linux 跑 `tsc -b`；这解决了 09-11 审查的 P1#1。**但从未被验证过能跑通**（无 push 记录）→ 建议先推到 GitHub 看一次 Actions 是否绿 |
| 5.2 | 🟠 `server.py:80-86` Token 用 `==` 比较 | 3 次普通字符串比较，应换 `secrets.compare_digest`（09-11 审查已提，仍未改） |
| 5.3 | 🟠 `main.cjs:34-35` `webSecurity:false` + `allowRunningInsecureContent:true` | `contextIsolation:true`/`nodeIntegration:false` 是对的，但后端已托管 SPA，主窗口改加载 `http://127.0.0.1:8000/` 即可恢复默认（09-11 已提，仍未改） |
| 5.4 | 🟠 `AGENTS.md` 让后来者读 `.workbuddy/` 与 `agents/`，但两者**都在 `.gitignore`** | 新克隆的机器两样都没有。`.workbuddy/memory/` 是你的私有工作记录（不该开源 ✅），但 `AGENTS.md` 的表述应改成「本机资产，不入库」 |
| 5.5 | 🟡 `web/src` 4 处硬编码 `http://127.0.0.1:8000` | `client.ts:23,30`、`useEffects.ts:37`、`useWorkshop.ts:258`（09-11 已提，仍未改） |
| 5.6 | 🟡 `AGENTS.md:43-49`「袋鼠音色素材铁律」含 B 站素材名与你自己的录制文件名 | 是可信的过程记录，但属素材来源指纹。可保留（不影响合规），或精简掉具体文件名 |

---

## 六、我建议的执行顺序（待你逐项确认）

### 第一批 · 必做（P0，开源前）✅ 2026-09-13 已完成

> 完成情况与实际改动范围见各条目下的 ✅ 说明。
> **残留风险已于 2026-09-13 13:18 解除**：`git filter-repo` 重写 master 清掉历史里的
> 证书密码明文，并核实旧对象不可恢复（详见 §3.1）。转公开的其它前提见 `docs/` 与本文件 §六末。

1. ✅ **3.1** 清除明文：3 处入库字面量全清（`release.ps1` / `release-sop.md` ×2），
   证书本体沿用；本文件里对我引用也一并打码；新增 `tools/check_secrets.py` + 测试按**内容**拦，防复发
2. ✅ **1.5** 删除 `extract_asar.cjs` / `probe_asar.cjs` / `repack_asar.cjs` / `tools/verify_asar_repack.cjs`，
   并同步改掉 6 处指向它们的**运维指令**（`docs/分发与打包说明.md` 的"复制进 .asar_tmp 再 repack"整段、
   `tools/sync_backend.ps1` 的提示、AGENTS.md、3 份 CODE_REVIEW_*）
3. ✅ **4.1** 修正 README 四处 `mobile/api.ts` → `mobile/src/api.ts` / `mobile/src/store.ts`（实际是 4 处，非 3 处）

### 第二批 · 强烈建议（P1）✅ 2026-09-13 已完成

4. ✅ **4.2** README router 数量已修正（实际 **24 个**，非 21；并补上漏列的 `ab_chain`/`market_api`/`pet_market_api`）
5. ✅ **3.3** 默认音色去硬编码：`RVC_DEFAULT_EXP` 默认改为空、`DEFAULT_REF` 改走 `VM_DEFAULT_REF`
   （未设则自动取 `tts_models/ref/` 下已有参考音）；作者本机的原值搬进**已 gitignore 的 `.env`**，行为不变
6. ✅ **2.1** `RvcDatasetCard` 现在把「检测失败」与「未就绪」分开，并用后端返回的 `exp` 渲染权重名
   （TS 类型里原本漏了 `exp` 字段，后端一直在返回它）
7. ✅ **5.2** Token 换 `secrets.compare_digest`（比 bytes，避免非 ASCII token 抛 TypeError）
8. ✅ **3.2B** `tools/cascade_acceptance.py` 补 `VM_RVC_ROOT` 覆盖

### 第三批 · 可选 / 转 issue（P2）

9. ✅ **5.3** Electron `webSecurity` 已恢复默认 —— 实测发现原"必须关"的前提**不成立**：
   后端 `LOCAL_ORIGIN_RE` 本就放行 `null` / `file://` 来源并回显 ACAO（连预检都放行），
   所以 file:// 页面走标准 CORS 即可，无需关同源策略。改动仅 2 行（删掉
   `webSecurity:false` + `allowRunningInsecureContent:true`），**不动窗口加载路径**，
   零启动风险；并补 `m2_server/tests/test_cors_file_origin.py` 把这根隐式耦合钉死
   （收紧 CORS 会让桌面端"能打开但接口全废"，而开发态 5173 同源复现不出来）。
10. ✅ **5.5** 前端硬编码后端地址已收敛：4 处 → `client.ts` 的 `backendPrefix()` 单点判定
   （`useEffects.ts` / `useWorkshop.ts` 改为复用）。顺带修掉一个**真实 bug**：
   `mediaUrl` 原先按 `import.meta.env.DEV` 判定，局域网用户（手机打开
   `http://<PC-IP>:8000`）跑构建产物、DEV 为 false，会拿到 `http://127.0.0.1:8000/...`
   —— 指向手机自己必然失败。判定依据改为"页面协议"。
   守卫见 `m2_server/tests/test_frontend_backend_origin.py`。
11. ✅ **4.5** `README-vm-test.md` → `docs/internal/`，并新增 `docs/README.md` 索引
12. ✅ **3.4** 内网 IP / 本机用户名已脱敏（`release-sop.md` 6 处、`AUDIO_TROUBLESHOOTING.md`、脚本注释）
13. ✅ **1.2/1.3/4.4** 日志统一 —— 按建议**只在 README 声明**（不动代码）：见 README
    「日志与排障」一节
14. ✅ **4.5** README「架构演进建议」剩余 3 条已转 issue（2026-09-13）：
    #1 移动端 mDNS 自动发现、#2 共享 API client 去重、#3 第三方素材名残留（含是否重写历史的决策）。

> 额外发现并处理（不在原清单里）：`.env` 竟然**没写进 `.gitignore`**（项目会加载它、也提供了
> `.env.example`）—— 已补。这类"以为有保护其实没有"的缺口与 §3.1 同源。

### 验证（改完必做，符合铁律 2）
```bash
CODEBUDDY_SAFE_DELETE_ENABLED=0 .venv/Scripts/python.exe -m pytest m2_server -q
CODEBUDDY_SAFE_DELETE_ENABLED=0 .venv/Scripts/python.exe -m ruff check m2_server tools --select F,E9
cd web && CODEBUDDY_SAFE_DELETE_ENABLED=0 npx tsc -b
```
推 GitHub 后确认 `.github/workflows/ci.yml` 真的绿。

**✅ 2026-09-14 已完成，但过程有反复（值得记）：**

第一次 CI 跑是**红的**（`9 failed, 626 passed, 3 skipped, 4 errors`），13 条里没有一条是
代码缺陷 —— 全是"开发机有、runner 没有"的资源（ffmpeg / `D:\RVC` / 微调产物 / 开发机屏幕
分辨率）。当时 `--ci-fidelity` 给的是**绿灯**，因为它只复刻了"依赖集"这一根轴。
完整复盘见 `docs/犯错指南.md` §3.10。

修完后：
- 本机全量 **656 passed**
- `--ci-fidelity`（干净 venv + 模拟裸 runner）**644 passed + 12 skipped**，前端 tsc 过
- GitHub Actions run [`34804680498`](https://github.com/mouxu66/voice-morph/actions/runs/34804680498)
  **两个 job 全绿**：后端 `652 passed, 4 skipped`、前端 `tsc -b` 通过。
  并已核对日志确认 **ffmpeg 真装上了**（`ffmpeg version 9.0.1-essentials_build`）、
  **0 条**"缺少本机资源 ffmpeg"跳过 —— 那 8 条依赖 ffmpeg 的用例是**真跑**的，
  不是靠 skip 换来的绿色。

---

## 附：本轮未覆盖的部分（诚实声明）

- `mobile/` 26,916 文件中 **26,886 个是 `node_modules`**，我只审了入库的 30 个源文件
- `tts_trial/`（9,109 文件）几乎全是第三方 venv，只抽查了入库的 16 个
- `_ref/`（163 文件，社区克隆参考）与 `seed_vc_repo/`（365 文件）**未审**——前者 `.gitignore` 已挡、后者已挡，不影响开源
- 未做依赖漏洞扫描（`pip-audit` / `npm audit`）——建议开源后挂 Dependabot
- 未审 `experiments/` 42 个 py（实验脚本，无生产影响）
