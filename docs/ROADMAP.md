# 技术增强路线图（增补调研）

> 更新时间：2026-09-02
> 来源：针对语音克隆/TTS 前沿方向的调研，落到本项目的 P0/P1/P2 增强路线；本文档用于固化清单、优先级、难度与依赖，供逐项推进时跟踪。

## 主线痛点（贯穿项）

**素材 → 音色 的成功率与易用性。**

其余痛点（表达力、多语言等）都是这条主线跑通之后的加分项。排优先级时，先问一句"它是否提升了从一段脏素材拿到可用音色的成功率，或减少了中间的手动判断"，否则降级。

把"成功"量化：

> 上传一段真实脏素材（多人 + BGM + 噪声）→ 一键跑完 → 得到 **≥30s 纯净同人**参考音频 → 零样本克隆可直接用；全程不需要人耳逐条试听。

### 子问题：已解 / 未解

| # | 子问题 | 对成功率的影响 | 现状 | 剩余缺口 |
|---|--------|----------------|------|----------|
| A | **多人混杂**：建库混入他人声，音色脏 | 高 | ✅ 已解 — 说话人分离 + 主说话人推荐（P0①） | 只在手动点「说话人分离」时触发，未进流水线默认链路 |
| B | **非人声污染**：BGM 残留、环境噪、混响、电音 | 高 | ⚠️ 部分 — demucs 去 BGM（`pipeline.step2_separate`） | 无残留检测、无降权/剔除，残留多时依然会进参考音频 |
| C | **录音质量问题**：削波、爆音、响度过低/过高、头尾或中段长静音 | 中高 | ⚠️ 部分 — `/clips` 只返回 `duration_s` / `loudness_dbfs` | 前端能看到但没有判定，靠人看数字 |
| D | **切片挑选成本高**：几十上百条逐个试听 | 中（易用性） | ❌ 未解 | 无打分、无排序、无默认勾选 |
| E | **失败不可诊断**：跑完才知道音色不行，不知卡在哪 | 中（易用性） | ❌ 未解 | 流水线只有进度，无"为什么不合格 + 怎么修" |

> 记：说话人分离只覆盖了 A（去混杂）。B/C/D/E 是**同一件事的两面**——先给切片打分（B/C），再用分数驱动自动优选（D）与失败解释（E），合并为 P1-1 一次性做完。

---

## P0 —— 已全部完成 ✅

| 项 | 内容 | 状态 | 落点 |
|----|------|------|------|
| P0① | 多说话人自动分离 + 主说话人推荐 | ✅ 已端到端验证（**代码未提交**） | `m2_server/speaker_sep.py`、`POST /clips/diarize`、`useWorkshop` 的 `runDiarize`/`speakerFilter`/`clipSpk` |
| P0② | 流水线单素材处理 | ✅ 已完成 | `POST /pipeline/run?file=…` + `find_ffmpeg()` 统一 ffmpeg |

---

## P1 —— 当前推进项

### P1-1 切片质检与自动优选（脏素材清洗 + 切片质量排序）

- **上提说明**：原 P2-1。因主线确认为「成功率 × 易用性」，而本项同时覆盖 B/C/D/E 四个未解子问题，是 P0 之后收益最高的一项，故上提为 P1 首个开工项。
- **目标**：脏素材不用人工逐条试听——切片自动打分、按分排序、不合格自动剔除，建库时自动挑够时长。
- **难度**：中。**状态**：🚧 已实现（2026-09-01），待真实脏素材端到端验收。

> **实测标定（奶龙合集 39 条 + 鲁豫专访 229 条）**：判废线按真实切片调过一轮——
> 「中段静音」从 40% 放宽到 60%（pipeline 按 ≥350ms 静音切片，3~10s 正常切片天然带
> 句间静音，实测好片普遍 0.27~0.35，40% 会误杀），并新增「有效语音占比 ≥25%」（原方案
> 漏判：整段静音/纯伴奏的切片因 mid_silence 区间被压成一点而判成 A）。声纹维度实测
> 把 2 条原本 A 级的他人声切片（sim 0.30 / 0.34）正确剔到 D。
> 性能：打分 229 条 0.8s；含声纹维度 39 条约 2.7s（CAM++ 首次加载另计，diarization
> 首跑 111s 含模型下载，之后走缓存）。

#### 1) 打分器 `m2_server/clip_qc.py`

对 `media/clips/*.wav` 逐条出指标，合成 0–100 分 + 等级 + 不合格原因列表：

| 维度 | 口径 | 权重 | 判废线 |
|------|------|------|--------|
| 时长 | 2.5–10s 合格（沿用 pipeline `CLIP_MIN_MS/MAX_MS`）| 20 | <1.5s 或 >12s |
| 响度 | `max_dBFS`，-20 ~ -3 dBFS 合格 | 15 | < -50 dBFS（沿用 `pipeline_clean` 判废阈值）|
| 削波 | \|x\|>0.99 采样占比 | 15 | >0.5% |
| 静音占比 | 能量 VAD，中段静音占比 | 15 | 中段静音 > 60%（实测标定，原 40% 会误杀）|
| 有效语音占比 | 能量 VAD 判定的语音帧占切片时长 | 硬判废（不加权）| < 25%（静音/纯伴奏兜底）|
| 底噪/SNR | 低分位能量 vs 语音段能量，粗估 SNR | 15 | SNR < 10 dB |
| 说话人一致性 | 与素材主说话人中心声纹的余弦相似度 | 20 | < 阈值 → **一票否决（D 级）** |

- 说话人一致性是**最强信号**，复用 `speaker_sep._sv_embed`（CAM++ 192 维）与 diarization 主说话人中心；相似度低的切片基本可判定为他人声或 BGM 残留，直接剔除。
- 语速（ASR 字数/秒 > 6.0 告警）作为**提示项不扣分**，阈值沿用 `tools/voice_qc.py: SPEED_WARN`；ASR 不可用则该维度置空、跳过。
- 等级：A 直接用 / B 可用 / C 谨慎（默认不选）/ D 剔除。
- 修复能力分两档：**必做**轻度修复（去直流、裁头尾静音、响度归一化）；**待评估**重度修复（去混响、去电音）——先只做"识别 + 剔除"，修复能力等真实脏样本标定后再加。

#### 2) 落盘与接口

- 按素材聚合落盘 `outputs/clip_qc/<prefix>.json`，缓存复用（切片文件 mtime 变化时重算）。
- `GET /clips` 响应带 `score / grade / reasons`，前端零额外请求即可排序、着色、一键「仅合格」。
- 新增 `POST /clips/qc?file=…` 手动重跑。

#### 3) 流水线衔接（易用性的关键）

- `/pipeline/run` 切片完成后**后台线程自动打分**（照抄 `rvc_live._maybe_run_qc` 的静默模式：失败静默、绝不阻断流水线），`PIPELINE_STATE` 里给出"合格 N / 总 M 条"。
- 说话人分离结果若已存在，直接复用其主说话人中心；否则先跑 diarize（A 与 B/C 合成一条链路，不再要求用户分两次点）。

#### 4) 建库自动优选

- `POST /voicebank` 支持 `auto=true`：在主说话人切片里按分取 top-N，累计到目标时长即停（零样本 ≥30s、精细档 ≥60s，沿用 `build_reference.py` 的口径），并回写选中的切片清单到 `clips.txt`。
- 前端仍保留手动勾选作为兜底。

#### 5) 验收口径

拿一段已知脏素材（含 BGM + 多人 + 噪声）一键跑完：

1. 自动选出的切片集合 **全部来自主说话人**、无削波、无长静音；
2. 累计时长 ≥30s；
3. 人工复核确认**无需再筛**即可建库；
4. 失败/剔除的切片在前端有可读原因（如"BGM 残留、声纹不一致"）。

#### 6') 落点（已实现）

- 打分器 `m2_server/clip_qc.py`（含命令行自查：`--prefix` / `--all` / `--force`）；
- `speaker_sep.main_center()` 提供主说话人中心声纹（质检的"说话人一致性"维度复用）；
- 服务端：`POST /api/clips/qc?file=&spk=1&force=0`、`GET /api/clips` 返回体带 `qc`、
  `/pipeline/run` 跑完后台自动打分（结果进 `PIPELINE_STATE.qc`）、
  `POST /api/voicebank` 支持 `auto=1&target_s=30` 自动优选；
- 前端：工坊素材卡片「质检」按钮、质检等级筛选（全部/合格/A/B/C/D）、切片按分降序、
  卡片上的等级徽标与不合格原因、「自动优选 30s」一键勾选。

#### 6) 风险与规避

- 阈值需按真实素材标定 → 先只展示分数、人工校准一轮，再开启自动剔除；
- CAM++ 打分是逐条推理，长素材切片多时耗时 → 后台线程 + 缓存 + 可选只对"疑似不合格"二次确认；
- 绝不阻断主流程：任何一步异常都降级为"该维度缺失"，不判废。

---

### P1-2 情感保留变声（Seed-VC）—— ✅ 已实现（2026-09-02）

- **目标**：让变声在不损失发音质量的前提下，尽可能保留**源语音**的语速、停顿、情感起伏，只替换音色。
- **覆盖痛点**：表达力（主线之外的加分项）。
- **调研结论（2026-09-01）**：

| 要素 | 结论 |
|------|------|
| 仓库/许可 | `github.com/Plachtaa/seed-vc`（已归档）· **GPL-3.0**（强 copyleft；仅本地自用无碍，分发 exe 需合规评估）|
| 推理方式 | **零样本风格/音色转换**：源语音 + 1–30s 参考音频 → 输出；V2=CFM+AR+ASTRAL 量化；无需像 RVC 那样训练 |
| 语义澄清 ⚠️ | 它的"情感保留"实为**参考音频驱动的风格迁移**：输出韵律偏向**参考音频**而非保留**源语音**；本实现按此语义提供「表达力变声」独立入口，不替代 RVC 的"逐帧保留源韵律" |
| 中文 / 显存 | 有中文评测（M4Singer CER 19.7% vs RVCv2 28.5%）；8GB 显存可跑（V2，权重缓存后单次约几十秒）|

- **形态（已实现）**：
  - 后端 `m2_server/seed_vc.py`：`POST /api/seedvc/run`（源音频 + 目标音色二选一：voicebank 音色 / 上传参考音；旋钮 `convert_style / similarity_cfg_rate / top_p / temperature / diffusion_steps / length_adjust / denoise`）+ `GET /api/seedvc/status`。推理在主 .venv 子进程跑 `seed_vc_repo/inference_v2.py`，权重走 HF 镜像缓存；单任务互斥，实时/级联变声中拒绝（抢 GPU 防护）。
  - `m2_server/offline_vc.py` 新增 `post_seedvc` 开关：RVC 转完再用 Seed-VC `convert_style` 把压平的韵律/情绪补回来（RVC 只保音高，表达力弱）。
  - 前端 `web/src/pages/SeedVc/`：离线工坊新增 Tab「表达力变声」（录音/上传源音频、目标音色、参数滑杆、进度轮询）；`OfflineVc` 页加「RVC 转完用 Seed-VC 补情绪/韵律」勾选。
  - 实验脚本 `experiments/seedvc_param_sweep.py`：固定源/参考音扫描 sim/top_p/temp，产出 A/B 试听 + 声纹/F0 指标报告（`outputs/seedvc_sweep/`）。
- **实测标定（2026-09-02，jay_0→trump_0，CAM++ 声纹）**：
  - `convert_style` 开 + **sim=0.5 最优**：像目标 0.807（全场最高）且漏源 0.122（最低），F0 表达力（std 18.9 / range 62）与源基线相当 → 前端默认 sim=0.5。
  - **top_p<0.6 崩坏**：top_p=0.5 输出无声垃圾（像目标 0.066、无有声帧、时长 32s）→ 前端下限锁 0.6。
  - sim=0.3 表达力最强（F0 std 30.8 / range 107）但漏源 0.24 最高；sim≥0.7 更贴参考音但 F0 被压回源水平。
- **状态**：✅ 已实现（worker 直调验证 + 参数扫描）。**残余项**：`/api/seedvc` 走完整 HTTP 的端到端待补跑；GPL-3.0 分发合规（本地自用无碍）待定。

---

## P2 —— 后续待办（按建议推进顺序）

### P2-1 失败可诊断 / 引导式修复

- 流水线与建库失败时给出可理解的原因与下一步建议（例："有效语音 <5s，换一段素材"、"主说话人占比 30%，素材以他人声为主"）。
- 覆盖痛点：易用性（主线 E 项）。依赖 P1-1 的打分结果。
- 难度：低（P1-1 之后基本是水到渠成）。状态：⏸ 未开始。

### P2-2 TTS 情感 / 节奏控制 —— ✅ 已完成

- **形态（已实现）**：「风格参考 ICL + 长文分段化」。TTS 页新增「朗读风格参考」下拉框，任选一个音色作为情感/节奏风格源，worker 用其短参考音频走 ICL 把目标文本分段（`VM_TTS_SEG_CHARS` 默认 60 字符，按句尾标点切段、超长句硬切）逐段合成后拼接，规避长文本单次 ICL 在 8GB 显存上的崩溃/乱叫。
- 关键约束落地：风格参考 ICL 仅接受≤10s 短参考（`VM_REF_MAX_S` 自动截断 + 文字稿按比例截断），弥补了"长文只能走 x-vector 声纹、无情感/节奏"的能力空缺；普通长文仍默认走 x-vector。
- 覆盖痛点：表达力。涉及端：`server.py`(`/tts` style_ref_voice)、`qwen3_tts.py`、`qwen3_tts_service.py`（`_split_segments`/`_tts_style_blocking`）、前端 `TtsPage`/`useTts`。状态：✅ 已端到端验证。

### P2-3 AI 音频溯源水印（AudioSeal）

- 本地接入音频溯源水印，输出合成/变声音频前可选打标，利于合规与可溯源。
- 难度：低；独立模块，验收直观。状态：⏸ 未开始。

### P2-4 多语言支持

- 扩展 TTS/ASR 的非中文能力，是否推进取决于目标语种的实际需要。
- 难度：中。状态：⏸ 未开始。

---

## 痛点覆盖矩阵

| 项 | 成功率 | 易用性 | 表达力 | 状态 |
|----|:------:|:------:|:------:|------|
| P0① 说话人分离 | ●●○ | ●●○ | – | ✅ |
| P0② 单素材流水线 | ●○○ | ●●○ | – | ✅ |
| **P1-1 切片质检与自动优选** | **●●●** | **●●●** | – | 🚧 已实现待验收 |
| P1-2 Seed-VC | ○○○ | – | ●●● | ✅ |
| P2-1 失败可诊断 | ○○○ | ●●○ | – | ⏸ |
| P2-2 TTS 情感/节奏 | ○○○ | ○○○ | ●●● | ✅ |
| P2-3 音频水印 | – | – | – | ⏸（合规）|
| P2-4 多语言 | ○○○ | ●○○ | ○○○ | ⏸ |
| P2-5 音频增强后处理 | ●●○ | ●○○ | – | ⏸（P1-1 之后）|
| P2-6 克隆效果可视化 | ○○○ | ●●○ | – | ⏸ |
| P2-7 多角色有声书 | – | ●○○ | ●●○ | ⏸ |

---

## 外部调研对照（2026-09-01 另一版调研，已与本项目实况对齐）

外部调研给了 12 项建议。逐项比对本项目实况后，结论分四类：

| 外部建议 | 本项目实况 | 结论 |
|---|---|---|
| ① pyannote 说话人分离 + 主说话人推荐 | **已落地**，且选型不同：`speaker_sep.py` 用魔搭 CAM++（Apache-2.0、免认证、国内直连）。pyannote 3.1 在 HF **gated**，需 token + 接受条款，与"离线/免认证"约束冲突 | ✅ 已做；**不换** pyannote |
| ② 流水线 `?file=` 只处理选定素材 | **已上线并提交**（`/pipeline/run?file=…` + 前端「仅处理此素材」） | ✅ 已做 |
| ③ 参考素材"可训练性"分级（SNR/语速/有效语音占比/混响） | 已有 `tools/voice_qc.py`，但打分对象是**训练后的音色**，不是**建库前的切片**；无 SNR/混响维度 | 🔁 与 P1-1 同源，按 P1-1 落地 |
| ④ 语速/语调/情绪控制（建议 OpenVoice V2 / GPT-SoVITS） | **已用 Qwen3-TTS 风格参考 ICL 实现并验证**（见 P2-2）。OpenVoice 早已被 Qwen3-TTS 取代（`demo_convert.py` 已废弃），GPT-SoVITS 训练版实测不如 Qwen3-TTS | ✅ 已做；**不回退**到 OpenVoice/GPT-SoVITS |
| ⑤ Seed-VC 零样本 VC | **已实现**（clone 于 `seed_vc_repo/`，P1-2 落地「表达力变声」入口 + RVC 后处理补情绪）。GPL-3.0 强 copyleft：仅本地自用无碍，分发 exe 需合规评估；"情感保留"语义以参考音驱动风格迁移实现 | ✅ 已做 |
| ⑥ 合成前降噪 / 去口水音 | **已有基础版**：离线变声链路 `offline_vc.py:101` 走 `ffmpeg afftdn`。缺模型级增强，且 TTS 参考音频侧没有 | 🆕 吸收为 P2-5 |
| ⑦ AudioSeal 水印 | 未做 | 🆕 即 P2-3 |
| P2 音效板（桌宠 + 全局热键） | 有 DSP 效果链（`effects.py`）但无快捷键触发 | 🆕 吸收为 P2-8 |
| P2 RVC ckpt-merge | 未做，增量小 | 🆕 吸收为 P2-9（低优先）|
| P2 多说话人对话（多角色有声书） | `audiobook.py` 目前**单 voice_id** | 🆕 吸收为 P2-7 |
| P2 GUI 可视化（波形/频谱/声纹散点） | 未做 | 🆕 吸收为 P2-6 |
| 明确不做：SVC 歌声、云端分发、VST/游戏集成 | 与本项目"纯本地工具"定位一致 | ✅ 采纳为红线 |

> 一句话：**调研版 P0 的 ①② 本项目已经做完，③ 与我们的 P1-1 是同一件事**；真正新增的是 ⑥ 降噪后处理与几个 P2 利基项。调研对"缺什么"的判断（成功率 > 表达力/易用性）与本项目主线完全一致。

### 新增吸收项

- **P2-5 音频增强后处理**（原调研 ⑥）：把现有 `afftdn` 升级为可选的模型级增强（DeepFilterNet / Resemble Enhance），覆盖三处入口——麦克风录入、TTS 参考音频、离线变声输入。前提：核对许可与离线可用性（模型需能一次性下载到本地）。难度：中。**与 P1-1 的关系：P1-1 决定"哪些切片配得上被增强"，本项决定"留下的切片/录音能修多好"，先 P1-1 后 P2-5。**
- **P2-6 克隆效果可视化**：波形/频谱/声纹散点对比（WaveSurfer 之类），回答"这次克隆到底像不像"。难度：中。
- **P2-7 多角色有声书**：`audiobook.py` 支持按段落/角色切换 voice_id。难度：低中。
- **P2-8 音效板**：全局热键 + 桌宠触发 meme 音效（复用 `effects.py`）。难度：低。
- **P2-9 RVC ckpt-merge**：模型融合出新音色。难度：低，增量小。

### 明确不做（红线）

- SVC 歌声转换（偏离定位）
- 云端音效库 / 社区分发（触碰"纯本地"红线）
- VST 插件 / 深度游戏集成（偏离"工具"定位）

---

## 推进约定

- **主线优先**：开工前先回答"它是否提升素材→音色的成功率或易用性"，否则降级到 P2。
- 每项开工前先完成可行性分析（显存/离线/许可/对接），确认后再写实现。
- 依赖与运行约束遵循项目全局约定（8GB 显存、离线、免费开源、自托管字体等）。
- 完成项与踩坑同步回写本项目记忆（`project_memory.md`）。
