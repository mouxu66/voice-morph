import { ChevronDown, CircleAlert, Download, History, Loader2, SlidersHorizontal, Sparkles } from "lucide-react"
import { TTS_MAX_LENGTH, type useTts } from "@/pages/Tts/useTts"
import { StudioAudioPlayer } from "@/components/voice-studio/StudioAudioPlayer"
import { StudioEmpty } from "@/components/voice-studio/StudioEmpty"
import { RvcDatasetCard } from "@/pages/Tts/RvcDatasetCard"

function formatTime(timestamp: number): string {
  return new Date(timestamp).toLocaleTimeString("zh-CN", { hour12: false })
}

export function TtsPage(p: ReturnType<typeof useTts>) {
  return <div className="min-h-full bg-gradient-to-br from-background via-background to-card"><header className="border-b border-border bg-card/30 px-5 py-10 sm:px-8 lg:px-12 lg:py-14"><div className="mx-auto max-w-7xl"><p className="font-mono text-xs uppercase tracking-widest text-primary">STAGE 04 / TEXT TO SPEECH</p><h2 className="mt-4 font-display text-4xl font-bold tracking-tight text-foreground sm:text-5xl">文字转语音</h2><p className="mt-4 max-w-2xl text-sm leading-6 text-muted-foreground">输入一段文字，让训练好的袋鼠音色把它念出来；支持试听、下载，本会话最近 5 条随取随听。</p></div></header><main className="mx-auto grid max-w-7xl gap-10 px-5 py-10 sm:px-8 lg:grid-cols-[minmax(0,1.6fr)_320px] lg:px-12 lg:py-14"><section className="space-y-8"><div className="rounded-2xl border border-border bg-card/85 p-5 shadow-lg backdrop-blur-xl sm:p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="font-mono text-xs uppercase tracking-widest text-primary">合成输入</p><h3 className="mt-2 text-lg font-semibold text-card-foreground">让袋鼠开口念</h3></div><span className={`font-mono text-xs ${p.overLimit ? "text-destructive" : "text-muted-foreground"}`}>{p.textLength} / {TTS_MAX_LENGTH}</span></div><textarea value={p.text} onChange={(event) => p.setText(event.target.value)} rows={6} placeholder="输入要让袋鼠开口念的话" className="mt-5 w-full resize-y rounded-md border border-border bg-background px-3 py-2.5 text-sm leading-6 text-foreground outline-none placeholder:text-muted-foreground focus-visible:ring-2 focus-visible:ring-primary" />{p.overLimit && <p className="mt-2 flex items-center gap-2 text-xs text-destructive"><CircleAlert className="h-3.5 w-3.5" />已超过 {TTS_MAX_LENGTH} 字上限，精简一点再合成。</p>}<div className="mt-5 rounded-lg border border-border bg-background/60"><button type="button" onClick={p.toggleAdvanced} aria-expanded={p.advancedOpen} className="flex w-full items-center justify-between px-4 py-3 text-xs font-medium text-muted-foreground transition hover:text-primary focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary"><span className="inline-flex items-center gap-2"><SlidersHorizontal className="h-3.5 w-3.5" />高级选项</span><ChevronDown className={`h-4 w-4 transition-transform ${p.advancedOpen ? "rotate-180" : ""}`} /></button>{p.advancedOpen && <div className="space-y-4 border-t border-border px-4 py-4"><div><label htmlFor="tts-prompt-text" className="text-xs font-medium text-muted-foreground">参考文本（可选）</label><input id="tts-prompt-text" value={p.promptText} onChange={(event) => p.setPromptText(event.target.value)} placeholder="一句话即可，辅助断句和语气，可留空" className="mt-2 w-full rounded-md border border-border bg-background px-3 py-2.5 text-sm text-foreground outline-none placeholder:text-muted-foreground focus-visible:ring-2 focus-visible:ring-primary" /></div><div><label htmlFor="tts-language" className="text-xs font-medium text-muted-foreground">文本语言</label><select id="tts-language" value={p.textLanguage} onChange={(event) => p.setTextLanguage(event.target.value)} className="mt-2 w-full rounded-md border border-border bg-background px-3 py-2.5 text-sm text-foreground outline-none focus-visible:ring-2 focus-visible:ring-primary"><option value="zh">中文</option><option value="en">英文</option></select></div></div>}</div>{!p.backendUp && <div className="mt-5 flex items-center gap-2 rounded-md border border-destructive/30 bg-destructive/10 px-3 py-2.5 text-xs text-destructive"><CircleAlert className="h-4 w-4" />本地服务离线，暂时无法合成，请确认后端已启动。</div>}{p.synthesizing && p.slowHint && <div className="mt-5 flex items-center gap-2 rounded-md border border-yellow-500/40 bg-yellow-500/10 px-3 py-2.5 text-xs text-yellow-600"><CircleAlert className="h-4 w-4" />{p.slowHint}</div>}{p.errorMessage && <div className="mt-5 flex items-center gap-2 rounded-md border border-destructive/30 bg-destructive/10 px-3 py-2.5 text-xs text-destructive"><CircleAlert className="h-4 w-4" />{p.errorMessage}</div>}<button type="button" disabled={!p.canGenerate} onClick={() => void p.generate()} className="mt-6 inline-flex w-full items-center justify-center gap-2 rounded-md bg-primary px-4 py-3 text-sm font-medium text-primary-foreground shadow-md transition hover:scale-105 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary disabled:pointer-events-none disabled:opacity-50 sm:w-auto">{p.synthesizing ? <Loader2 className="h-4 w-4 animate-spin" /> : <Sparkles className="h-4 w-4" />}{p.synthesizing ? "正在合成…" : "生成语音"}</button></div>{p.latestResult ? <div className="rounded-2xl border border-border bg-card/85 p-5 shadow-lg backdrop-blur-xl sm:p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="font-mono text-xs uppercase tracking-widest text-primary">合成结果</p><h3 className="mt-2 text-lg font-semibold text-card-foreground">最新一条语音</h3></div><a href={p.latestResult.url} download={`tts-${p.latestResult.id}.wav`} className="inline-flex items-center gap-2 rounded-md border border-primary/40 bg-primary/10 px-3 py-2 text-xs font-medium text-primary transition hover:bg-primary/20 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary"><Download className="h-4 w-4" />下载音频</a></div><p className="mt-4 line-clamp-2 text-sm leading-6 text-muted-foreground">{p.latestResult.text}</p><StudioAudioPlayer src={p.latestResult.url} label="播放合成语音" className="mt-4" /></div> : <StudioEmpty title="还没有合成结果" description="在上方输入文字并点击生成语音，袋鼠会把这段话说给你听。" />}<RvcDatasetCard /></section><section><div className="sticky top-24 rounded-2xl border border-border bg-card p-5 shadow-lg sm:p-6"><p className="font-mono text-xs uppercase tracking-widest text-primary">会话历史</p><h3 className="mt-2 flex items-center gap-2 text-xl font-semibold text-card-foreground"><History className="h-4 w-4 text-primary" />最近 5 条</h3>{p.ttsHistory.length ? <ul className="mt-5 space-y-3">{p.ttsHistory.map((item, index) => <li key={item.id} className="rounded-lg border border-border bg-background/60 p-3"><div className="flex items-center justify-between gap-2"><span className="font-mono text-xs text-primary">{String(index + 1).padStart(2, "0")}</span><span className="rounded-full bg-muted px-2 py-0.5 text-xs text-muted-foreground">{item.language === "zh" ? "中文" : "英文"}</span></div><p className="mt-2 line-clamp-2 text-xs leading-5 text-card-foreground">{item.text}</p><StudioAudioPlayer src={item.url} label="回放这条语音" className="mt-3" /><p className="mt-2 font-mono text-xs text-muted-foreground">{formatTime(item.createdAt)}</p></li>)}</ul> : <p className="mt-5 text-xs leading-5 text-muted-foreground">还没有合成记录，生成一条就会出现在这里。记录会保存在本机，刷新后仍然保留。</p>}</div></section></main></div>
}
