import { Check, ChevronRight, CircleAlert, FileVideo2, Filter, FolderOpen, Library, Loader2, Play, Scissors, SlidersHorizontal, Sparkles, Upload, X } from "lucide-react"
import { Link } from "react-router-dom"
import type { useWorkshop } from "@/pages/Workshop/useWorkshop"
import { StudioAudioPlayer } from "@/components/voice-studio/StudioAudioPlayer"
import { StudioEmpty } from "@/components/voice-studio/StudioEmpty"

const stages = ["批量初筛", "精细审核", "送入音色库"]
const QUALITY_LABEL: Record<string, string> = { 推荐: "响度达标", "需检查": "响度偏低" }

function ProgressRing({ progress }: { progress: number }) {
  const radius = 44
  const circumference = 2 * Math.PI * radius
  const dash = circumference * (Math.min(100, progress) / 100)
  return <div className="relative h-28 w-28 shrink-0"><svg viewBox="0 0 104 104" className="h-full w-full -rotate-90" aria-label={`采纳素材进度 ${progress}%`}><circle cx="52" cy="52" r={radius} fill="none" stroke="currentColor" strokeWidth="8" className="text-muted" /><circle cx="52" cy="52" r={radius} fill="none" stroke="currentColor" strokeWidth="8" strokeLinecap="round" className="text-primary transition-[stroke-dasharray] duration-500" strokeDasharray={`${dash} ${circumference}`} /></svg><div className="absolute inset-0 flex flex-col items-center justify-center"><span className="font-mono text-xl font-semibold text-card-foreground">{progress}%</span><span className="text-xs text-muted-foreground">30–60 秒目标</span></div></div>
}

export function WorkshopPage(p: ReturnType<typeof useWorkshop>) {
  const progress = Math.min(100, Math.round((p.selectedDuration / 60) * 100))
  const markVisible = (decision: "采纳" | "驳回") => p.visibleClips.forEach((clip) => p.setDecision(clip.name, decision))
  const pipelineActive = p.pipeline.running || p.pipeline.status === "running"

  return <div className="min-h-full bg-gradient-to-br from-background via-background to-card"><header className="border-b border-border bg-card/30 px-5 py-10 sm:px-8 lg:px-12 lg:py-14"><div className="mx-auto max-w-7xl"><p className="font-mono text-xs uppercase tracking-widest text-primary">STAGE 01 / SOURCE REVIEW</p><h2 className="mt-4 font-display text-4xl font-bold tracking-tight text-foreground sm:text-5xl">音色工坊</h2><p className="mt-4 max-w-2xl text-sm leading-6 text-muted-foreground">把视频素材拆成可用的人声片段，在质检台里试听、筛选，再送入音色库。</p><div className="mt-8 flex items-center gap-2 overflow-x-auto pb-1">{stages.map((stage, index) => <div key={stage} className="flex shrink-0 items-center gap-2"><div className={`flex h-8 w-8 items-center justify-center rounded-full border font-mono text-xs ${index === 0 || (index === 1 && p.reviewMode === "精审") ? "border-primary bg-primary text-primary-foreground" : "border-border bg-card text-muted-foreground"}`}>{index + 1}</div><span className={`text-xs ${index === 0 ? "font-medium text-foreground" : "text-muted-foreground"}`}>{stage}</span>{index < stages.length - 1 && <ChevronRight className="h-4 w-4 text-muted-foreground" />}</div>)}</div></div></header><main className="mx-auto max-w-7xl space-y-12 px-5 py-10 sm:px-8 lg:px-12 lg:py-14"><section className="grid gap-5 lg:grid-cols-[minmax(0,2fr)_320px]"><div className="rounded-2xl border border-border bg-card/85 p-5 shadow-lg backdrop-blur-xl sm:p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="font-mono text-xs uppercase tracking-widest text-primary">素材来源</p><h3 className="mt-2 text-lg font-semibold text-card-foreground">准备质检用的原始视频</h3></div><div className="flex flex-wrap items-center gap-2">
      <button type="button" onClick={p.openRawFolder} className="inline-flex items-center gap-2 rounded-md border border-border bg-background px-3 py-2 text-xs text-muted-foreground transition hover:border-primary hover:text-primary"><FolderOpen className="h-3.5 w-3.5" />打开素材文件夹</button>
      <button type="button" disabled={!p.backendUp || pipelineActive} onClick={() => void p.startPipeline()} className="inline-flex items-center gap-2 rounded-md bg-primary px-4 py-2.5 text-sm font-medium text-primary-foreground shadow-md transition hover:scale-105 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary disabled:pointer-events-none disabled:opacity-50">{pipelineActive ? <Loader2 className="h-4 w-4 animate-spin" /> : <Sparkles className="h-4 w-4" />}{pipelineActive ? "处理中" : "运行流水线"}</button>
    </div></div>

    {/* 分步进度条 */}
    {pipelineActive && <div className="mt-5 rounded-lg border border-primary/30 bg-primary/10 px-4 py-3">
      <div className="flex items-center justify-between gap-3">
        <div className="flex min-w-0 items-center gap-2 text-xs text-primary"><Scissors className="h-4 w-4 shrink-0" /><span className="truncate">{p.pipeline.message || "处理中…"}</span></div>
        <span className="shrink-0 font-mono text-xs text-primary">{Math.round(p.pipeline.percent)}%</span>
      </div>
      <div className="mt-2 h-1.5 overflow-hidden rounded-full bg-primary/20"><div className="h-full rounded-full bg-primary transition-[width] duration-500" style={{ width: `${Math.max(2, p.pipeline.percent)}%` }} /></div>
      <button type="button" onClick={() => void p.stopPipeline()} className="mt-3 inline-flex items-center gap-1.5 rounded-md border border-primary/40 px-2.5 py-1.5 text-xs text-primary transition hover:bg-primary/10"><X className="h-3 w-3" />取消本次处理</button>
      {p.pipeline.status === "cancelled" && <p className="mt-2 text-xs text-muted-foreground">已取消，可稍后重试。</p>}
    </div>}

    {p.feedback && <div className="mt-4 flex items-center gap-2 rounded-md border border-primary/30 bg-primary/5 px-3 py-2.5 text-xs text-primary animate-in fade-in slide-in-from-top-2 duration-300"><CircleAlert className="h-3.5 w-3.5" />{p.feedback}</div>}
    {p.errorMessage && <div className="mt-4 flex items-center gap-2 rounded-md border border-destructive/30 bg-destructive/10 px-3 py-2.5 text-xs text-destructive"><CircleAlert className="h-3.5 w-3.5" />{p.errorMessage}</div>}

    {/* 素材列表 + 拖拽上传 */}
    <div className="mt-6 space-y-3">
      {p.videos.length ? <div className="grid gap-3 sm:grid-cols-2">{p.videos.map((video) => <div key={video.name} className="flex min-w-0 items-center gap-3 rounded-lg border border-border bg-background/60 p-3"><FileVideo2 className="h-5 w-5 shrink-0 text-primary" /><div className="min-w-0"><p className="truncate text-sm text-card-foreground">{video.name}</p><p className="mt-1 font-mono text-xs text-muted-foreground">{video.size_mb.toFixed(1)} MB · 已就绪</p></div></div>)}</div> : <StudioEmpty title="还没有视频素材" description="拖拽视频到下方区域，或点击选择文件上传，也可以打开素材文件夹手动放入。" onRetry={p.loadWorkshop} />}
      <div
        role="button"
        tabIndex={0}
        onClick={() => p.fileInputRef.current?.click()}
        onKeyDown={(e) => { if (e.key === "Enter" || e.key === " ") { e.preventDefault(); p.fileInputRef.current?.click() } }}
        onDragOver={(e) => { e.preventDefault(); p.setDragging(true) }}
        onDragLeave={() => p.setDragging(false)}
        onDrop={(e) => { e.preventDefault(); p.setDragging(false); void p.handleFiles(e.dataTransfer.files) }}
        className={`flex cursor-pointer flex-col items-center justify-center rounded-xl border-2 border-dashed px-4 py-6 text-center transition ${p.dragging ? "border-primary bg-primary/5" : "border-border bg-background/50 hover:border-primary/60"}`}
      >
        {p.uploading ? <><Loader2 className="h-6 w-6 animate-spin text-primary" /><p className="mt-2 text-xs text-muted-foreground">上传中 {Math.round(p.uploadProgress * 100)}%…</p></> : <><Upload className="h-6 w-6 text-primary" /><p className="mt-2 text-xs text-muted-foreground"><span className="font-medium text-card-foreground">拖拽视频到这里</span> 或点击选择 · 支持 mp4/mkv/mov/flv/webm/avi</p></>}
      </div>
      <input ref={p.fileInputRef} type="file" accept=".mp4,.mkv,.mov,.flv,.webm,.avi,video/*" multiple className="hidden" onChange={(e) => void p.handleFiles(e.target.files)} />
    </div>
  </div><aside className="rounded-2xl border border-border bg-card p-5 shadow-lg sm:p-6"><p className="font-mono text-xs uppercase tracking-widest text-primary">质检摘要</p><div className="mt-6 flex items-center gap-5"><ProgressRing progress={progress} /><div><p className="text-sm font-medium text-card-foreground">{p.selectedItems.length} 段已采纳</p><p className="mt-1 font-mono text-xs text-muted-foreground">{p.selectedDuration.toFixed(1)} / 60.0 sec</p><p className="mt-3 text-xs leading-5 text-muted-foreground">建议保留 30–60 秒干净人声，越多越像。</p></div></div><div className="mt-6 border-t border-border pt-4 text-xs text-muted-foreground"><div className="flex items-center justify-between"><span>可送入音色库</span><span className="font-mono text-card-foreground">{p.selectedItems.length} 段</span></div><Link to="/voices" className="mt-4 flex w-full items-center justify-center gap-2 rounded-md border border-primary/40 bg-primary/10 px-4 py-2.5 text-xs font-medium text-primary transition hover:bg-primary/20 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary"><Library className="h-4 w-4" />进入音色库<ChevronRight className="h-4 w-4" /></Link></div></aside></section><section><div className="mb-5 flex flex-col gap-4 xl:flex-row xl:items-end xl:justify-between"><div><p className="font-mono text-xs uppercase tracking-widest text-primary">片段质检台</p><h3 className="mt-2 text-2xl font-semibold text-foreground">逐条听，留下最干净的声音</h3><p className="mt-2 text-sm text-muted-foreground">真实波形试听；「响度达标」仅代表音量合适，是否纯人声请以听感为准。</p></div><div className="flex flex-wrap items-center gap-2"><div className="flex items-center gap-1 rounded-md border border-border bg-card p-1"><Filter className="ml-2 h-3.5 w-3.5 text-muted-foreground" />{(["全部", "推荐", "需检查"] as const).map((filter) => <button key={filter} type="button" onClick={() => p.setQualityFilter(filter)} className={`rounded-sm px-3 py-1.5 text-xs transition ${p.qualityFilter === filter ? "bg-primary text-primary-foreground" : "text-muted-foreground hover:text-foreground"}`}>{filter === "推荐" ? "响度达标" : filter === "需检查" ? "响度偏低" : filter}</button>)}</div><button type="button" onClick={() => p.setReviewMode(p.reviewMode === "精审" ? "初筛" : "精审")} className="inline-flex items-center gap-2 rounded-md border border-border bg-card px-3 py-2 text-xs text-muted-foreground transition hover:border-primary hover:text-primary"><SlidersHorizontal className="h-3.5 w-3.5" />{p.reviewMode === "精审" ? "精审中 · 按 A 采纳 / R 驳回" : "进入精审"}</button><button type="button" onClick={() => markVisible("采纳")} className="rounded-md border border-border bg-card px-3 py-2 text-xs text-muted-foreground transition hover:border-primary hover:text-primary">批量采纳</button><button type="button" onClick={() => markVisible("驳回")} className="rounded-md border border-border bg-card px-3 py-2 text-xs text-muted-foreground transition hover:border-destructive hover:text-destructive">批量驳回</button><button type="button" onClick={() => void p.exportRvc()} className="inline-flex items-center gap-2 rounded-md border border-primary/40 bg-primary/10 px-3 py-2 text-xs font-medium text-primary transition hover:bg-primary/20"><Scissors className="h-3.5 w-3.5" />导出 RVC 训练集</button></div></div>{p.loading ? <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{[1, 2, 3].map((item) => <div key={item} className="h-48 animate-pulse rounded-2xl border border-border bg-card" />)}</div> : p.visibleClips.length ? <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{p.visibleClips.map((clip) => { const quality = clip.loudness_dbfs >= -18 && clip.loudness_dbfs <= -8 ? "推荐" : "需检查"; const selected = p.selectedClips.has(clip.name); const decision = p.decisions[clip.name]; return <article key={clip.name} className={`rounded-2xl border bg-card p-4 shadow-md transition hover:-translate-y-0.5 hover:shadow-lg ${selected ? "border-primary/60" : "border-border"}`}><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="truncate text-sm font-medium text-card-foreground">{clip.name}</p><p className="mt-1 font-mono text-xs text-muted-foreground">{clip.duration_s.toFixed(1)}s · {clip.loudness_dbfs.toFixed(1)} dBFS</p></div><span className={`shrink-0 rounded-full px-2 py-1 text-xs ${quality === "推荐" ? "bg-primary/10 text-primary" : "bg-muted text-muted-foreground"}`}>{QUALITY_LABEL[quality]}</span></div><StudioAudioPlayer src={p.clipAudioUrl(clip)} className="mt-4" /><div className="mt-4 flex gap-2"><button type="button" onClick={() => p.setDecision(clip.name, "采纳")} className={`flex flex-1 items-center justify-center gap-2 rounded-md border px-3 py-2 text-xs transition ${decision === "采纳" || selected ? "border-primary bg-primary/10 text-primary" : "border-border text-muted-foreground hover:border-primary hover:text-primary"}`}><Check className="h-3.5 w-3.5" />采纳</button><button type="button" onClick={() => p.setDecision(clip.name, "驳回")} className={`flex flex-1 items-center justify-center gap-2 rounded-md border px-3 py-2 text-xs transition ${decision === "驳回" ? "border-destructive bg-destructive/10 text-destructive" : "border-border text-muted-foreground hover:border-destructive hover:text-destructive"}`}><X className="h-3.5 w-3.5" />驳回</button></div></article> })}</div> : <StudioEmpty title="当前筛选下没有片段" description="放宽筛选条件，或先运行素材流水线。" onRetry={p.loadWorkshop} />}</section><section className="flex flex-col gap-4 rounded-2xl border border-primary/30 bg-gradient-to-r from-primary/10 via-card to-card p-5 shadow-md sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-medium text-card-foreground">片段整理完成后，去音色库创建参考音频</p><p className="mt-1 text-xs text-muted-foreground">当前已选 {p.selectedItems.length} 段，共 {p.selectedDuration.toFixed(1)} 秒（建议 30–60 秒）</p></div><div className="flex items-center gap-2 text-xs text-primary"><Play className="h-4 w-4" />下一站：音色库</div></section></main></div>
}