# 语音模型选型对比报告

> 目标：评估是否需要把「变身工坊」当前的语音引擎换成大厂/新开源模型。
> 约束（你的选择）：**质量优先、体积无所谓、先只做调研不改代码**。
> 调研日期：2026-08-26（已核实小红书 dots.tts、千问 Qwen3-TTS 等最新开源情况）

---

## 0. 你现在的家底

| 功能 | 当前引擎 | 用法 | 痛点 |
|------|----------|------|------|
| **变声/转换**（素材流水线、麦克风） | **RVC** | 需先训一个专属底模 | 轻量、实时生态成熟 |
| **文字转语音 TTS** | **GPT-SoVITS (v4)** | 需先训 `meituan_rat` 专属模型，再用 `_audio_polish.py` 修长停顿/低电平 | 要训练、质量不稳、需后处理 |

**关键认知**：大厂开源模型 90% 是 **TTS（文字→语音）**，不是 **VC（实时变声）**。所以"整体更换"不成立，应该 **分模块看**。

---

## 0.5 关于"大厂开源语音"的现状澄清（重要）

常被误传的几点，已逐一核实：

| 厂商 | 开源语音模型 | 权重是否开源 | 说明 |
|------|--------------|--------------|------|
| **阿里（通义）** | **两支**：`CosyVoice3`（FunAudioLLM 团队）+ `Qwen3-TTS`（千问 Qwen 团队） | ✅ 都开源 | 同一家两个不同团队，都进入候选 |
| **小红书** | `dots.tts`（联合上海交大） | ✅ 开源 | 2026-08-13 开源，20 亿参数连续自回归 TTS，开源 SOTA 级 |
| **百度（文心）** | 无语音大模型开源 | ❌ | 开源的是文心**文本**大模型 ERNIE 4.5；语音仍是 PaddleSpeech + 闭源文心语音 API |
| **字节（豆包）** | Seeduplex 全双工语音 | ❌ 不开源权重 | 只做"生态/工具开源"，权重拿不到，只能走火山引擎 API |
| **腾讯 / 智谱 等** | 多为闭源 API | ❌ | 语音大模型基本以云端 API 形式提供，不能本地离线部署 |

**结论**：能**本地离线部署**的开源语音大模型，大厂里目前就 **阿里 CosyVoice3 + 阿里 Qwen3-TTS + 小红书 dots.tts** 三个。它们都已纳入本报告的候选范围，你并没有错过什么。闭源 API 类（豆包、文心语音等）对"桌面双击、本地拉起后端"的应用用不了，不列为候选。

> 补充：`Qwen2.5-Omni` 是千问的全模态**对话**模型（语音只是其中一环，能实时语音聊天），不是专门的音色克隆/变声工具，适合以后做"语音助手对话"，对你当前单点任务不合适，本报告不列为主候选。

---

## 1. 文字转语音 TTS 候选对比

| 模型 | 来源 | 克隆方式 | 中文质量 | 多语言 | 推理显存 | 模型体积 | 备注 |
|------|------|----------|----------|--------|----------|----------|------|
| **GPT-SoVITS v4**（当前） | 社区 | 需训练专属模型 | 中上 | 中 | ~4 GB | 小（但需训练流程） | 长停顿/低电平问题，靠 `_audio_polish` 兜底 |
| **IndexTTS-2** | Index | zero-shot（免训练） | **很高（中文口碑最佳之一）** | 中 | ~6–8 GB | 数 GB | 2026 中文 TTS 横评常居前列 |
| **CosyVoice3** | 阿里 FunAudioLLM | zero-shot 3 秒 | 高 | **18+ 语言** | ~6–8 GB | 数 GB | 自然语言指令控情感/方言，工业级 |
| **Qwen3-TTS** | 阿里千问 Qwen | zero-shot 3 秒 | **高（阿里自家中文强）** | 多语种 | ~6–8 GB（待核实） | 数 GB | 2026-01 开源，支持自然语言控声 + **低延迟流式**，极契合本项目文字转语音+克隆 |
| **dots.tts** | 小红书+上海交大 | 连续自回归（推测 zero-shot，需核实） | **新开源 SOTA 级** | 待核实 | ~4 GB | 数 GB | 2026-08-13 刚开源，路线新（非离散 token），非常新需实测 |
| **Fish Speech** | Fish Audio | zero-shot | 高 | 多语言 | ~6–8 GB | 数 GB | 部署最简单，但偶有底噪需后处理 |
| **F5-TTS** | 上海 AI Lab | zero-shot | 高 | 多语言 | ~4–6 GB | 小–中 | 实时性最好；长文本偶有怪调需调参 |

**结论（TTS）**：你当前要"先训袋鼠专属模型"这一步，换成以上任意 zero-shot 模型都能**直接给一段袋鼠音频做克隆，免训练**。质量、稳定性都明显更好。体积无所谓 → 都可选项。其中 **Qwen3-TTS / CosyVoice3** 同属阿里、中文强且支持流式/控声；**dots.tts** 最新鲜但未经实战；**IndexTTS-2** 中文口碑稳。建议各拉权重跑同一段袋鼠音频盲测。

---

## 2. 变声 VC（实时/素材转换）候选对比

| 模型 | 类型 | 零样本 | 实时麦克风 | 推理显存 | 备注 |
|------|------|--------|------------|----------|------|
| **RVC**（当前） | 训练式 VC | 否（需训底模） | **可近实时，链路成熟** | ~2–4 GB | 轻量、生态最成熟，你应用核心 |
| **Seed-VC** | 零样本扩散 VC | **是（1–30 秒参考音频）** | **支持实时 GUI** | ~4–6 GB | 2026 年 VC 最值得关注；免去训练底模 |
| **OpenVoice** | 零样本 VC | 是 | 否（离线） | ~4 GB | 音色好但实时性弱 |
| **CosyVoice（VC 模式）** | 零样本 | 是 | 流式（非麦克风实时） | ~6–8 GB | TTS 为主，VC 是附带 |

**结论（VC）**：大厂模型几乎**没有**能替代 RVC「实时麦克风 + 素材流水线」的成熟方案。唯一的亮点是 **Seed-VC**——零样本、支持实时、无需训练底模，质量可能高于你现在的 RVC，但集成进现有链路要重新做（实时延迟、采样率适配）。

> **关于 OpenVoice 的状态澄清**：你的「变身工坊」**从未使用过 OpenVoice**——当前变声引擎是 RVC，TTS 是 GPT-SoVITS。OpenVoice 只是本次调研列出的**候选之一**，并非被替换掉的旧方案。它在零样本 VC 里音色不错，但因**不支持实时**、且同类的 **Seed-VC 更强**（支持实时、质量更好），所以**不是 VC 升级的首选**；只有当你明确"只做离线变声、不要实时"，它才值得考虑。

---

## 3. 针对你这个应用的落地建议

### 阶段 1（低风险、收益最大）：TTS 升级
- 把 `gptsovits_tts.py` 的 GPT-SoVITS 换成候选之一：**Qwen3-TTS / CosyVoice3 / IndexTTS-2 / dots.tts**。
- 好处：免训练、质量高、彻底去掉 `_audio_polish` 那套补丁。
- 袋鼠音色：直接拿现有 `002.wav`（或任意袋鼠片段）做 zero-shot 即可，不用再训 `meituan_rat` 模型。
- RVC 变声核心**完全不动**。
- 四家里：`Qwen3-TTS`/`CosyVoice3` 最全面且中文强、支持流式；`IndexTTS-2` 中文口碑稳；`dots.tts` 最新需实测。建议先各拉权重跑同一段袋鼠音频盲测。

### 阶段 2（可选、中风险）：变声引擎评估
- 评估 **Seed-VC** 替换 RVC 做素材转换，免去训练底模流程。
- 注意：实时链路要重新适配，且显存翻倍（4→6 GB）。若你只在意"素材流水线转换"而非实时麦克风，Seed-VC 零样本更省事；若在意实时，RVC 仍更稳。

### 不建议做的
- 整体"换成大厂模型"——VC 没有合适替代，强行换只会退化实时能力。
- 追闭源 API（豆包/文心语音）—— 无法离线本地部署，与你的桌面应用架构冲突。
- 用 Qwen2.5-Omni 这类全模态对话模型做单点 TTS/VC——语音只是附带能力，质量与可控性不如专用模型。

---

## 4. 部署成本提醒（虽体积无所谓，但仍有边界）

- 大模型首次拉权重会慢（数 GB 下载），桌面应用自动拉起后端时要预热。
- 显存：TTS 升级到上述模型需 ~4–8 GB 空闲显存；你当前 `cuda:true`（有 GPU），需确认剩余显存够同时跑 TTS + RVC。
- 打包：模型走本地权重目录，不塞进 asar，避免安装包膨胀。

---

## 4.5 社区口碑与试跑优先级（2026-08 补充）

（基于社区实测/教程密度/已知坑梳理，用于决定先下哪几个）

| 模型 | 社区热度 | 部署成熟度 | 已知坑 | 试跑优先级 |
|------|----------|------------|--------|------------|
| **IndexTTS-2**（B站） | 最高（CSDN/B站/知乎大量实战） | 高，**4GB 显存即可跑** | 相对少；偏新但教程齐全 | ⭐⭐⭐ **首推** |
| **CosyVoice3**（阿里） | 高（有一键运行包） | 高 | 情感指令偶尔不稳定 | ⭐⭐⭐ **备选首推** |
| **Qwen3-TTS**（阿里千问） | 中（权重 2026-01 才开源） | 中（实战教程较少） | 部署文档相对少 | ⭐⭐ 第二阶段 |
| **dots.tts**（小红书） | 低（2026-08-13 才开源，仅 2 周） | 低 | 太新、低资源语言缺陷、有安全管控 | ⭐ 暂不试 |

**社区共识要点**：
- IndexTTS-2：音色-情感解耦 + 字级时长精准控制 + 中文拼音校正强，中文质量第一梯队，最适合配音/试听。
- CosyVoice3：情感拟人、自然语言指令控情感/方言，与 IndexTTS-2 是「精准 vs 情感」两种风格。
- dots.tts：EmergentTTS-Eval 上情绪表现力开源最佳、句法复杂度超闭源，但发布太新、坑未知。

**结论：先下 IndexTTS-2 + CosyVoice3 两个做袋鼠音色 zero-shot 盲测**，足够覆盖「精准/情感」两类需求且社区最成熟、坑最少；Qwen3-TTS 与 dots.tts 留作第二阶段，等前两者定了再考虑。

## 5. 下一步

确认方向后我可以：
1. 先帮你**拉 Qwen3-TTS / CosyVoice3 / IndexTTS-2 / dots.tts 权重 + 写个最小推理脚本**验证袋鼠音色 zero-shot 效果；
2. 再决定是只换 TTS，还是连 RVC 一起评估 Seed-VC。

（本报告未改动任何代码。）
